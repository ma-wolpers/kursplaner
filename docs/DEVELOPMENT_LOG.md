# Development Log (kursplaner)

Dieses Dokument trackt technische Aenderungen fuer Feature- und Architekturarbeit.

Regel:
- Keine Feature- oder Architekturaenderung ohne Update in diesem Log.
- Bugfix-Only-Changes koennen ohne Eintrag erfolgen.

## [Unreleased]

### Added (2026-08-27) — Jahrgangsstufenabhängige Achievement-Struktur (Phase 2b, noch ohne Fach-Vorgaben)

**Kein Changelog-Eintrag**: diese Änderung ist reine Infrastruktur ohne aktuell sichtbaren Effekt — `resources/achievements/grade_requirements.json` enthält für alle vier Fächer bewusst leere Regel-Listen (keine erfundenen Platzhalterwerte), sodass sich am Achievement-Tab nichts ändert, solange keine echten Vorgaben eingetragen sind (durch `test_query_ub_achievements_without_grade_requirements_repo_adds_no_grade_items`/`..._with_empty_requirements_adds_no_grade_items` abgesichert).

**Architektur (Ports/Adapter, analog zu `KompetenzkatalogRepository`)**: neues Protokoll `AchievementGradeRequirementsRepository` (`core/ports/repositories.py`) mit `load_requirements() -> dict[str, tuple[GradeRequirement, ...]]`; konkrete Implementierung `FileSystemAchievementGradeRequirementsRepository` (`infrastructure/repositories/`) liest/parst die JSON-Ressource. Reine Rechenlogik liegt getrennt in `core/domain/achievement_grade_rules.py` (`GradeRequirement`, `GradeGroupProgress`, `parse_grade_requirements`, `compute_grade_group_progress`) — keine I/O im Domain-Layer.

**Zentrale Fachliste nicht dupliziert**: die JSON-Ressource verwendet als Top-Level-Schlüssel exakt die Strings aus `QueryUbAchievementsUseCase.DOMAIN_ORDER` (bereits bestehende, maßgebliche Fachliste) — `test_default_resource_file_keys_match_domain_order` verhindert stillen Drift zwischen beiden Stellen.

**Bestehende current/target-Semantik unverändert wiederverwendet, nicht neu erfunden**: jede konfigurierte Jahrgangsstufen-Gruppe wird zu einem zusätzlichen, ganz gewöhnlichen `AchievementProgress`-Item über die bestehende `_achievement()`-Methode (`current` = rohe, auf `target` gekappte Trefferzahl, `is_fulfilled = current>=target`, exakt wie bei den bestehenden half/full/ubplus/bub-Kategorien) — kein neues UI-Konzept, keine zweite Item-Liste. `_achievement()` bekommt dafür einen neuen optionalen Parameter `label_override`, da die Kachel-Beschriftung bislang aus einer domainübergreifend statischen `CATEGORY_LABELS`-Zuordnung kam, Jahrgangsstufen-Gruppenlabels aber pro Fach unterschiedlich sind (kein Verhaltensunterschied für bestehende Aufrufer, `label_override` ist überall sonst `None`).

**Ein Fach ohne konfigurierte Gruppen erzeugt keine zusätzlichen Items** (`_append_grade_group_items()` bricht bei leerem `requirements`-Tupel sofort ab) — kein `0/0`-Fake-Fortschritt, keine neue "nicht konfiguriert"-UI-Zustandsart nötig.

**Zeilenlimit**: `query_ub_achievements_usecase.py` lag bereits bei 263/300 Zeilen — die eigentliche Berechnung wurde deshalb bewusst nach `achievement_grade_rules.py` ausgelagert statt inline erweitert; die Datei liegt danach bei ~273 ausführbaren Zeilen.

**Tests**: `tests/test_achievement_grade_rules.py` (Parsing/Berechnung), `tests/test_achievement_grade_requirements_repository.py` (Laden, fehlende Datei, Fachlisten-Abgleich), `tests/test_query_ub_achievements_usecase.py` (kein Item ohne Repo/bei leeren Vorgaben, korrekte current/target/Label-Zuordnung bei konfigurierten Vorgaben, andere Fächer bleiben unberührt). 665/665 Tests grün.

**Nächster Schritt** (separat, Datenänderung statt Codearbeit): Rückfrage an den Nutzer nach den konkreten Grenzen/Mindestanzahlen je Fach, danach `grade_requirements.json` befüllen und diesen Changelog-Eintrag nachträglich um einen `CHANGELOG.md`-Eintrag ergänzen.

### Added (2026-08-27) — Jahrgangsstufe als UB-Datenfeld (Phase 2a der Achievement-Jahrgangsstufen-Vorbereitung)

**Neuer YAML-Key `Jahrgangsstufe`** auf UB-Dateien ([`unterrichtsbesuch_policy.py`](kursplaner/core/domain/unterrichtsbesuch_policy.py), analog zu `UB_YAML_KEY_BEREICH`). Neue reine Funktion `parse_jahrgangsstufe(value) -> int | None`: numerisch 5-13 (dieselbe Konvention wie `core/domain/grade_groups.py` und das Informatik-Kompetenzkatalog-Manifest), alles andere (fehlend, leer, nicht-numerisch, außerhalb 5-13) liefert `None` statt einer Exception — Altbestand ohne dieses Feld bleibt dadurch ladbar.

**Wichtig — `required_keys` bewusst NICHT erweitert**: `ub_repository.py::_UB_SCHEMA.required_keys` prüft beim Laden auf Vorhandensein der Keys und wirft sonst `RuntimeError` — `UB_YAML_KEY_JAHRGANGSSTUFE` wurde deshalb nur zu `_UB_ORDERED_KEYS` (Schreib-Reihenfolge beim Speichern) hinzugefügt, nicht zu `required_keys`, damit bestehende UB-Dateien ohne dieses Feld weiterhin ladbar bleiben (manuell verifiziert: Laden einer UB-Datei ohne den Key erzeugt keinen Fehler, `parse_jahrgangsstufe(None)` liefert `None`).

**GUI**: `ub_mark_dialog.py::UbMarkDialog` bekommt ein optionales Zahlenfeld „Jahrgangsstufe (5-13, optional)" neben Beobachtungsschwerpunkt; ungültige (nicht-leere, aber nicht parsebare) Eingabe wird wie die bestehende „mindestens eine UB-Art"-Prüfung mit `messagebox.showerror` abgelehnt, statt stillschweigend verworfen zu werden. `action_controller.py::_ub_dialog_defaults_for_day()` liest den Wert für bestehende UB-Verknüpfungen als Dialog-Default (zwei Aufrufstellen: Bearbeiten-Dialog und die Detail-Anzeige beim Löschen einer verlinkten Einheit).

**Tests**: `tests/test_course_subject_and_ub_policy.py` (`parse_jahrgangsstufe`-Wertebereich/Fehlerfälle), `tests/test_mark_unit_as_ub_usecase.py` (Schreiben eines Werts, sowie Regressionstest für den unveränderten Fall ohne Wert). 651/651 Tests grün.

### Added (2026-08-27) — Sonderziele + AFB-Hinweis im Erwartungshorizont

**Neues Feld `Sonderziele`**: Liste wie `Teilziele`, in `lesson_yaml_policy.py` (`LIST_FIELDS`, `_ALLOWED_BY_TYPE["Unterricht"]` direkt nach `Teilziele`, Default `[]`). Fachliche Abgrenzung zu `Teilziele`: Teilziele stecken den verpflichtenden Weg aller Lernenden zum Stundenziel ab (AFB I/II), Sonderziele sind zusätzliche, freiwillige Ziele für schnellere/interessierte Lernende — bewusst ein eigenständiges Feld statt einer Markierung bestehender Teilziele, u. a. weil sie im Erwartungshorizont-Export separat (kursiv) erscheinen sollen.

**GUI**: Sonderziele nutzen durchgehend dieselbe generische „Zeilenfelder"-Infrastruktur wie Teilziele (kein eigenes Dialog-Widget nötig) — ergänzt in `row_display_mode_usecase.py` (`UNTERRICHT_ROWS`, `list_like_fields()`), `lesson_edit_usecase.py::set_lesson_field()`, `lesson_context_controller.py` (Anzeigeformatierung), `grid_renderer.py::_field_help_text()`. Tooltip-Text in `help_catalog.py`, dort auch der AFB-Hinweis bei `stundenziel`/`teilziele` ergänzt: „Teilziele stecken AFB I/II ab, das Stundenziel sollte darüber liegen (AFB III: Transfer/Vertiefung)" — **reiner Hinweistext, keine Validierung, keine Speicher-Blockade, keine automatische Ableitung**.

**Erwartungshorizont-Export**: `ExpectedHorizonLine.is_main_goal: bool` durch `kind: GoalKind` (`STUNDENZIEL | TEILZIEL | SONDERZIEL`, neues Enum in `export_expected_horizon_usecase.py`) ersetzt — kein zusätzliches Bool-Feld, da genau zwei Konsumenten (Markdown- und PDF-Renderer) dieselbe Unterscheidung von zwei auf drei Zustände erweitern mussten (per Grep verifiziert, keine weiteren Verbraucher). `_goals_for_day()` hängt `Sonderziele` nach `Teilziele` an. `expected_horizon_markdown_renderer.py` bekommt `_italic()` neben `_bold()`, das interne Zeilen-Tupel führt `kind` statt `is_main_goal`; `_strip_markdown_wrappers()` erkennt jetzt zusätzlich einfache `*...*`-Kursivmarker (Reihenfolge `("**", "~~", "*")` wichtig, damit `**fett**` nicht fälschlich als „außen kursiv" erkannt wird), damit der AFB/Aufg/Pkte-Carry-Over beim erneuten Export weiterhin funktioniert. `expected_horizon_pdf_renderer.py` bekommt einen `_cell_italic_style` (Helvetica-Oblique) und denselben Drei-Wege-Stil-Entscheid.

**Nebeneffekt (bewusst, kein Bug)**: Ist `Stundenziel` leer, aber `Teilziele` gesetzt, wurde die erste Zeile bisher rein positionsbedingt fett dargestellt (`is_main_goal = index==0`). Mit `kind` wird nur noch die tatsächliche `Stundenziel`-Zeile fett markiert — kein bestehender Test deckte den alten, rein positionsbedingten Fall ab.

**Tests**: `tests/test_lesson_yaml_policy_ub_field.py` (Default/Erhalt von `Sonderziele`), `tests/test_export_expected_horizon_usecase.py` (Reihenfolge/`kind`-Zuordnung inkl. Sonderziele), `tests/test_expected_horizon_markdown_renderer.py` (kursive Markdown-Ausgabe), manueller PDF-Rendering-Smoketest mit allen drei `GoalKind`-Werten. 648/648 Tests grün.

**Nebenbei korrigiert**: `README.md`s Einheiten-Dateien-Feldliste war bereits vor dieser Änderung unvollständig (führte `Teilziele` nicht auf, obwohl seit längerem Schema-Feld) — beim Ergänzen von `Sonderziele` direkt mitkorrigiert, da dieselbe Liste betroffen war.

### Removed (2026-08-27) — Migrationsleichen der alten Stem-Semantik entfernt

**Kontext**: Bei der Planung einer Kopierfunktions-Anpassung stellte sich heraus, dass Einheiten-Dateinamen seit der Umstellung auf `generate_random_lesson_stem()` reine 6-stellige Zufallscodes ohne semantischen Inhalt sind — Lerngruppe/Datum/Thema werden nirgends mehr aus dem Dateistamm abgeleitet oder abgeleitet werden sollen. Eine repoweite Aufrufer-Analyse (alle Verwendungen von `build_lesson_stem`/`build_regular_stem`/`parse_*_token`) ergab sechs vollständig unerreichbare Symbole aus der Zeit vor dieser Migration, die entfernt wurden:

- `MainWindow._parse_group_token()`, `_parse_subject_token()`, `_parse_grade_token()`, `_build_regular_stem()` (`adapters/gui/main_window.py`): vier delegierende Wrapper-Methoden ohne jeden Aufrufer (repoweit geprüft) — der tatsächliche, weiterhin aktive Aufrufer der Funktion „Einheit umbenennen" (`action_controller.py::rename_selected_lesson`) geht direkt über `lesson_context_controller`, nicht über diese Wrapper.
- `LessonContextController.parse_subject_token()`, `.parse_grade_token()` (`adapters/gui/lesson_context_controller.py`): einzig von den nun entfernten `main_window`-Wrappern referenziert, sonst ohne Aufrufer.
- `PasteLessonUseCase._unit_title_from_lesson_stem()` und `MarkUnitAsUbUseCase._unit_title_from_lesson_stem()` (jeweils eigene, identische Kopie): leiteten einen „Einheitstitel" aus dem Dateistamm ab (Annahme: Schema „Gruppe mm-dd Inhalt") — das berechnete Ergebnis (`unit_title`) wurde an beiden Aufrufstellen nie gelesen, reine tote Berechnung, seit Zufallscode-Stems zusätzlich inhaltlich bedeutungslos.

**Bewusst nicht angefasst**: `build_lesson_stem`/`build_regular_stem`/`parse_group_token`/`parse_mmdd`/`row_mmdd` (`core/domain/lesson_naming.py`, `adapters/gui/lesson_context_controller.py`) bleiben unverändert — sie tragen weiterhin die aktive Funktion „Einheit umbenennen" (`action_controller.py:1752`). Deren Zukunft (behalten/auf `Stundenthema`-Update umstellen/entfernen) ist eine eigenständige, hier bewusst nicht getroffene Produktentscheidung.

**Tests**: keine Verhaltensänderung, da alle sechs Fundstellen nachweislich ohne Aufrufer waren — 644/644 Tests weiterhin grün, keine neuen Tests nötig.

### Changed (2026-08-25) — "Naechste Einheit" auf Startzeit umgestellt, persistente Markierung, nummerische Navigation

**Kernsemantik**: "Naechste Einheit" = die erste stattfindende Einheit, deren Startzeit noch nicht erreicht ist (statt bisher: reiner Datumsvergleich im Grid bzw. fester globaler 15:00-Cutoff in der Kursuebersicht). Einziger Wahrheitsort: `core/domain/next_unit_policy.py::unit_counts_as_upcoming()` (reines Domain-Modul, kein GUI/Tk) -- Grid-Markierung/-Auswahl (`overview_controller._next_lesson_column_index`), Kursuebersicht (`PlanOverviewQueryUseCase._is_row_upcoming`, betrifft `next_unit`/`next_topic`/`next_lzk`/`remaining_hours`/`next_ub`) und nummerische Spaltenauswahl nutzen ausnahmslos dieselbe Funktion. Fehlende Startzeit (kein Rhythmus-Eintrag) gilt bewusst konservativ als anstehend. Der `reference_day`-Determinismus-Bypass in `summarize_plan` bleibt unveraendert rein datumsbasiert (Docstring + Regressionstest halten das fest).

**Opt-in globaler Cutoff**: Neue Settings-Sektion "Naechste Einheit" (Dropdown "Startzeit der Einheit"/"Feste Uhrzeit", Default Startzeit; Stunde/Minute-Felder nur im Cutoff-Modus sichtbar). Eigene, von `ub_past_cutoff_time` unabhaengige Keys (`next_unit_mode`, `next_unit_cutoff_time`) in `ui_preferences_store.py` -- anderes Konzept ("Einheit noch anstehend" vs. "UB bereits abgehalten").

**bw_gui-Erweiterung** (`a:\Code\bw-gui`): `SettingsFieldSpec.visible_when: tuple[controlling_key, required_value] | None` fuer bedingte Feld-Sichtbarkeit im Tabbed-Settings-Dialog -- generisch, nicht kursplaner-spezifisch. Minimal-invasiv ueber das bestehende Section-Rebuild-Muster geloest (Trace auf dem steuernden Feld loest ein Re-Render der aktiven Sektion aus, `_render_field` wertet `visible_when` bei jedem Aufruf neu aus), kein separates Show/Hide-Widget-System.

**`PlanOverviewQueryUseCase`**: neuer `now_provider`-Parameter ersetzt einen direkten `datetime.now()`-Aufruf in `summarize_plan` (Testbarkeit, kein verstecktes Zeit-Coupling in der Query-Schicht). `next_ub`s Startzeit wird ueber `ub_date` aufgeloest (kann von `row_date` abweichen, z. B. wenn ein UB-Termin nicht mit dem Plan-Tag der Einheit zusammenfaellt) -- eigener Regressionstest deckt diesen Fall ab (haette mit `row_date` faelschlich das falsche Rhythmus-Segment gewaehlt).

**Persistente Header-Markierung**: `grid_renderer.py` berechnet den Next-Unit-Index NICHT selbst (`GridRenderer.set_next_unit_index()` ist ein reiner Setter, kein Controller-Aufruf) -- `main_window.py`s Grid-Render-Wrapper (`_rebuild_grid`, `_refresh_grid_content`, `_update_grid_column`, `_update_grid_header`) berechnen ihn einmal pro Aufruf und reichen ihn durch. Markierung nur am Spaltenkopf (kein Vollspalten-Rahmen) ueber zwei getrennte Kanaele: Hintergrund-`col_type` `"next_unit"` (Token `info_soft`) im Normalfall, angehaengtes `▶`-Glyph als Fallback wenn der Hintergrund bereits durch cancel/hospitation/lzk/unresolved belegt ist -- kollidiert dadurch nie mit dem unveraenderten UB-/Datumslos-Rahmen (`_apply_ub_border`).

**Nummerische Spaltenauswahl**: neue Zifferntasten `0`-`9` (`SHORTCUT_SELECT_UNIT_BY_OFFSET`), nur aktiv im Spaltenauswahlmodus der Detailansicht (`allow_when_text_input=False` + `SELECTION_LEVEL_COLUMN`-Guard im Intent-Handler, analog `SHORTCUT_DETAIL_LEFT_ALL`). `selection_controller.select_unit_at_offset_from_next(offset)`: `0` = aktuelle naechste Einheit, `1`-`9` = die N-te folgende stattfindende Spalte, Ausfall-Tage uebersprungen, kein Wrap-around, No-op wenn Anker oder Ziel-Offset fehlen. Existierte vorher nicht (Repo-weit auf Kollisionsfreiheit von `0`-`9` geprueft).

**Nebenfund, nicht mitgefixt**: `query_ub_plan_usecase.py` nutzt fuer die "kommende UBs"-Liste einen hartkodierten 15:00-Default statt des konfigurierten `ub_past_cutoff_time`-Werts -- unabhaengiges, bereits vorbestehendes Ungleichgewicht, separat zu klaeren.

**Tests**: neue `next_unit_policy`-Testmatrix (Vergangenheit/Zukunft/vor-bei-nach Startzeit/fehlende Startzeit/Cutoff-Modus), erweiterte `test_next_lesson_column_index.py`/`test_plan_overview_with_index.py` (Zeitfaelle, `now_provider`, `reference_day`-Bypass-Regressionstest, `next_ub`-Startzeit-Aufloesung), neue `test_grid_renderer_next_unit_header.py` (alle Marker-Kanal-Kombinationen), neue `select_unit_at_offset_from_next`-Tests und Shortcut-Gating-Tests. `bw_gui`: eigene `visible_when`-Tests.

### Fixed (2026-08-25) — Naechste-Einheit-Markierung: DayColumn-Migration unvollstaendig

**Bug**: `MainWindowOverviewController._next_lesson_column_index()` (`adapters/gui/overview_controller.py`) rief weiterhin `day_info.get("is_cancel", False)`/`day_info.get("datum", "")` auf — Rest des `dict`-basierten Zwischenstands vor der `DayColumn`-Objektkapselung (`c737e2c`, 2026-08-15). Diese Migration hat `overview_controller.py` selbst nur bei der Typannotation von `_project_visible_day_columns` beruehrt, den Koerper von `_next_lesson_column_index()` aber uebersehen (dessen `>=`-Datumsvergleich und Fallback-Logik sind seitdem unveraendert). `DayColumn` ist ein frozen Dataclass ohne `.get()` → `AttributeError` bei jedem Aufruf, sobald `day_columns` nicht leer war. Betraf: automatische Markierung der naechsten Einheit beim Oeffnen eines Kursplans (`load_selected_table`), Auto-Scroll dorthin (`_scroll_to_next_unit`), sowie den Pfeiltasten-Navigationsstart ohne aktive Auswahl (`selection_controller.move_selection_to_adjacent{,_occurring}`).

**Testluecke**: `test_selection_controller.py` und `test_row_filter_navigation.py` stubben `_next_lesson_column_index` komplett via `SimpleNamespace(_next_lesson_column_index=lambda: 0)` weg — die echte Implementierung lief in keinem bestehenden Test gegen echte `DayColumn`-Instanzen. Repo-weiter Grep ueber alle `DayColumn`-Feldnamen als String-Key bestaetigt, dass dies die einzigen zwei verbliebenen dict-artigen Zugriffe im gesamten Baum waren (der Migrationscommit selbst hatte drei aehnliche Bugs an anderer Stelle bereits behoben und korrekt umgestellt).

**Fix**: `day_info.get("is_cancel", False)` → `day_info.is_cancel()`, `day_info.get("datum", "")` → `day_info.datum`. Zusaetzlich den zuvor ungeschuetzten Aufruf in `load_selected_table` eng mit einem stillen `except Exception`-Guard umschlossen (bestehendes Pattern aus `new_course_window.py`/`screen_builder.py` fuer optionale UI-Anreicherungen), damit ein kuenftiger Fehler in der Next-Unit-Berechnung nur die Markierung verliert statt den gesamten Kursplan-Load abzubrechen.

**Tests**: Neue Datei `tests/test_next_lesson_column_index.py` fuehrt die echte Methode gegen echte `DayColumn`-Instanzen (via `tests/day_column_factory.py::make_day_column`) — leere Spaltenliste, zukuenftige/heutige Einheit, Ausfall-Spalten, Fallback bei ausschliesslich vergangenen Daten, unparsierbares Datum. 600/600 Tests gruen.

### Fixed/Changed (2026-08-19) — Inhalt/Thema-Ausfall-Spaltenindex-Bug + Dataview-Links + Spalten-Kapselung

**Bug**: `LessonTransferUseCase.relink_row_to_stem`/`.replace_or_append_first_link` schrieben hartkodiert auf `table.rows[row_index][2]` — Rest eines alten 4-Spalten-Schemas (`Datum|Stunden|Inhalt|Thema/Ausfall`), in dem `Inhalt` Index 2 hatte. Seit Entfernung der `Stunden`-Spalte (siehe CHANGELOG "Stunden-Spalte der Plantabelle entfällt") liegt `Inhalt` auf Index 1 — beide Methoden wurden dabei nicht mitgezogen. Effekt: `PlanRegularLessonUseCase.execute_write` ruft `sync_thema_ausfall_to_plan_row` (schreibt den korrekten Themenfolge-Link nach `Thema/Ausfall`) und direkt danach `relink_row_to_stem` auf, das denselben `Thema/Ausfall`-Zellwert über den `[2]`-Bug sofort wieder überschrieb.

**Feature**: `Inhalt`-Zellen verlinken Einheiten jetzt über eine Dataview-Inline-Query (`wiki_links.py::build_dataview_lesson_link`, Format `` `= link("stem", [[stem]].Stundenthema)` ``) statt eines rohen `[[stem]]`-Links — Obsidian zeigt dadurch das `Stundenthema`-YAML-Feld der Einheit live als Linktext an. `sync_thema_ausfall_to_plan_row`s Themenfolge-Link (`Thema/Ausfall`-Spalte) bleibt unverändert `build_wiki_link`-basiert. Manuelle Zell-Edits in der Haupttabelle (`SaveCellValueUseCase._compose_inhalt_value`) verwerfen jetzt getippten Text zugunsten des kanonischen Dataview-Links, sobald für die Zeile ein Link existiert — es gibt kein Alias-Konzept mehr, da die Anzeige live aus `Stundenthema` kommt (`_sanitize_link_alias`/`_alias_from_stem` entfernt, Root-Cause-Cleanup statt totem Code).

**Architektur**: `PlanTableData` (`core/domain/plan_table.py`) bekommt echte Spaltenzugriffs-Kapselung (`column_index`/`column_index_optional`/`inhalt`/`set_inhalt`/`thema_ausfall`/`set_thema_ausfall`) plus kanonische Spaltennamen-Konstanten (`COLUMN_DATUM`/`COLUMN_INHALT`/`COLUMN_THEMA_AUSFALL`), referenziert jetzt auch von `plan_table_markdown_io.py::_locate_plan_table_block`s Header-Validierung (vorher zwei unabhängige String-Listen). Löst 4 near-identische Duplikate ab (`plan_commands_usecase._header_map`/`_idx`, `convert_to_hospitation_usecase._header_index`, `mark_unit_as_ub_usecase._header_index`, `lesson_edit_usecase._header_map`) sowie mehrere weitere Inline-Vorkommen in `plan_table_file_repository.py` und `content_markers.py` (Letzteres nur String-Konstante statt Methodenaufruf, da die Funktionen dort mit rohen `headers`/`row`-Listen statt `PlanTableData` arbeiten). **Bewusst nicht migriert** (außerhalb des Aufgabenschnitts, reine Lesepfade unbeteiligter Spalten wie `Datum`/`Stunden`): `archive_past_lesson_files_usecase.py`, `lesson_naming.py`, `lesson_context_query_usecase.py`, `load_plan_detail_usecase.py`, `paste_lesson_usecase.py`, `plan_overview_query_usecase.py`, `query_ub_plan_usecase.py`, `rename_linked_file_for_row_usecase.py`, `editor_controller.py`, `course_lifecycle.py`.

**Aufräum-Runde (YAML/Markdown-Duplikate)**: `yaml_registry.py` bekommt `body_after_frontmatter(raw_text)` (löst 3 unabhängige Hand-Roll-Duplikate der Frontmatter-Grenzerkennung ab: `plan_table_file_repository.save_linked_lesson_yaml`/`.set_lesson_markdown_sections`, `ub_repository.load_ub_markdown`) und `render_yaml_frontmatter(ordered_keys, values)` (löst 2 Duplikate ab: `plan_table_file_repository._render_yaml_frontmatter`, `ub_repository._render_yaml_frontmatter`; inkl. der dort doppelt definierten `WIKI_LINK_VALUE_RE`/`MARKDOWN_LINK_VALUE_RE`, jetzt nur noch hier). Neues Modul `core/domain/markdown_lists.py::render_markdown_bullet_section(title, items)` löst die strukturell duplizierte "## Titel + Bullets"-Komposition zwischen `plan_table_file_repository.set_lesson_markdown_sections` (Wiki-Link-Items, Section nur bei nicht-leerer Liste) und `ub_repository._render_list_section` (Klartext-Items, Platzhalter-Bullet bei Leerheit) ab — beide Policy-Unterschiede bleiben Aufrufer-seitig (UB übergibt `[""]` statt `[]`, um den alten Platzhalter-Bullet zu reproduzieren).

**`preserve_alias`**: `LessonTransferUseCase.relink_row_to_stem`s `preserve_alias`-Parameter bleibt auf der öffentlichen Signatur (Aufrufer-Stabilität für `action_controller.py`/`lesson_context_controller.py`), Methodenkörper vereinfacht, da beide Zweige seit Umstellung auf Dataview-Links identische Ausgabe liefern (kein Alias-Konzept mehr) — Docstring hält das explizit fest.

**300-Zeilen-Limit**: `save_cell_value_usecase.py` fiel durch das Entfernen von `_sanitize_link_alias`/`_alias_from_stem` von ~346 auf ~292 ausführbare Zeilen — der Ausnahme-Eintrag in `docs/ARCHITEKTUR_KERN.md` §28 wurde entfernt, da die Datei jetzt unter dem Limit liegt.

**Tests**: 594/594 grün (32 neue). Fixture-Korrektur (nicht nur Assertion-Patch) in `test_convert_to_hospitation_usecase.py`, `test_rename_linked_file_for_row_usecase.py`, `test_wiki_links.py`, `test_paste_lesson_usecase_ub_copy.py`, `test_paste_lesson_conflict_delete_ub_cleanup.py`, `test_mark_unit_as_ub_usecase.py` — synthetisches `["Datum","Stunden","Inhalt"]`-Schema (verdeckte den `[2]`-Bug, weil `Inhalt` dort zufällig auf Index 2 lag) durch echtes `["Datum","Inhalt","Thema/Ausfall"]` ersetzt. Neue direkte Tests für `relink_row_to_stem`/`replace_or_append_first_link` (`test_lesson_transfer_usecase.py`, zuvor ungetestet). Neuer End-to-End-Regressionstest `test_plan_regular_lesson_usecase.py`, der beweist, dass `Thema/Ausfall` einen kompletten Anlage-/Update-Zyklus über `PlanRegularLessonUseCase.execute_write` übersteht (zuvor durch keinen Test abgedeckt) — sowohl für den "Link existiert bereits" als auch den "Link wird im selben Flow neu angelegt"-Pfad. Neue Unit-Tests für `PlanTableData`-Methoden (`test_plan_table.py`), `build_dataview_lesson_link` (`test_wiki_links.py`), `body_after_frontmatter`/`render_yaml_frontmatter` (`test_yaml_registry.py`, neue Datei), `render_markdown_bullet_section` (`test_markdown_lists.py`, neue Datei).

### Fixed/Added (2026-08-16) — Schulweite Ausfälle: Nacharbeiten aus erster Nutzung

Vier Nacharbeiten, nachdem der Nutzer das frisch committete Feature (`36b6b62`) erstmals mit echten Daten benutzt hat.

**A) Popup-Schließ-Glitch (bw-gui, `cec2f0a`)**: Gemeldet als "Widgets werden beim Schließen sichtbar abgesaugt". Wahrscheinliche, nicht vollständig bewiesene Ursache: `ScrollablePopupWindow._request_close()` rief `self.destroy()` direkt auf einem `Toplevel` mit verschachteltem `Panedwindow`/scrollbarer Canvas auf — das sequenzielle Zerstören der Kind-Widgets löst dabei sichtbares Reflow/Kollabieren aus, bevor der Toplevel selbst verschwindet. Fix: `self.withdraw()` vor `self.destroy()` — Standard-Tk-Pattern, macht das Teardown unsichtbar unabhängig von der genauen Ursache. Da `_request_close()` über mehrere Pfade erreichbar ist (WM-Protokoll, Escape), wurde ein `self._closing`-Guard ergänzt, der einen zweiten `destroy()`-Aufruf auf einem bereits zerstörten Widget verhindert (`TclError`). Zentral in `bw-gui` gefixt (betrifft alle Popups, nicht kursplaner-lokal).

**B) Datumsfelder in `school_wide_cancellation_dialog.py`**: Jahr ist jetzt optional — neue reine Hilfsfunktion `parse_flexible_date(text, *, today=None)` nutzt bei fehlendem Jahr `(today or date.today()).year`. Bewusst per Regex (`_DAY_MONTH_RE`) statt `strptime("%d.%m.")` implementiert: `strptime` ohne Jahr fällt intern auf ein nicht-Schaltjahr zurück, wodurch "29.02." im tatsächlichen Zieljahr fälschlich als ungültig abgelehnt worden wäre, selbst wenn dieses ein Schaltjahr ist (Tests: `test_parse_feb_29_without_year_in_leap_target_year`/`..._non_leap_target_year_is_none`). Neue reine Funktion `resolve_date_range(von_text, bis_text, *, today=None) -> tuple[date, date] | None`: leeres Bis-Feld → eintägiger Ausfall (`date_to = date_from`); `Bis < Von` → `None` (explizite Fehlermeldung statt stillem negativen/leeren Zeitraum). `_bis_var` startet bei neuen Einträgen jetzt leer statt mit dem heutigen Datum; beim Bearbeiten eines bestehenden Eintrags wird `entry.date_to` weiterhin immer explizit gesetzt.

**C) Neue/verlängerte Kurse übernehmen aktive schulweite Ausfälle automatisch**: `CreatePlanUseCase` bleibt bewusst unangetastet (keine Kopplung an schulweite Ausfälle) — stattdessen neuer Nachbearbeitungsschritt `core/usecases/apply_school_wide_cancellations_to_new_rows_usecase.py::ApplySchoolWideCancellationsToNewRowsUseCase`, aufgerufen nach erfolgreicher Planerzeugung aus `action_controller.py::create_new_lesson_tracked()`/`extend_plan_to_next_vacation()`. Neue reine Funktion `core/domain/school_wide_cancellation.py::claims_for_grade(entries, *, grade_level, date_from, date_to)`: pro Datum gewinnt der erste in Listenreihenfolge (= Erstellungsreihenfolge) passende Entry — explizit getestete Semantik, keine implizite Sortierung (`tests/test_claims_for_grade.py`). Der Usecase markiert ausschließlich Zeilen, die weder Ferien-/Feiertag- noch Ausfall-markiert sind UND leeren Inhalt haben (nie vorhandenen Inhalt überschreiben); erzeugt für jede markierte Zeile einen `UnitMove(reference=None, target=None)` — derselbe bestehende Ledger-Mechanismus wie bei inhaltslosen Zeilen im regulären Apply-Usecase, kein neues Ledger-Modell nötig. Dadurch greifen Revert/Edit/Delete/Diagnostics ohne Sonderbehandlung.
- **Schreibreihenfolge bewusst umgekehrt** gegenüber dem regulären Apply/Revert: hier zuerst Store, danach Plantabelle. Begründung: schlägt der Plantabellen-Schreibvorgang nach dem Store-Schreibvorgang fehl, ist der verwaiste Ledger-Eintrag über den bestehenden Diagnose-Usecase erkennbar (`resolve_move` → `ERROR`); in umgekehrter Reihenfolge wäre eine verwaiste Markierung ohne Ledger-Referenz dagegen unsichtbar. Kein neuer Transaktionsmechanismus, nur eine begründete Reihenfolge-Entscheidung (getestet in `tests/test_apply_school_wide_cancellations_to_new_rows_usecase.py`, Store-gelingt/Plan-schlägt-fehl-Fall).
- **Fehlerbehandlung**: `NewLessonUseCase`/`ExtendPlanToNextVacationUseCase` selbst sind bereits abgeschlossen, bevor der neue Schritt beginnt — ein Fehler dort wird an der Aufrufstelle (`action_controller.py`) separat per `messagebox.showwarning(...)` gemeldet, ohne den umgebenden `_run_tracked_write`-Schritt scheitern zu lassen und ohne den bereits erfolgreichen Kurs "teilweise" zurückzulassen. Läuft der Schritt erfolgreich, wird seine Plandatei-Änderung automatisch Teil desselben Undo-Schritts (bestehender `extra_after`-Mechanismus, keine neue Verdrahtung).

**D) Speicherort konfigurierbar + außerhalb des Repos**: `config/schulweite_ausfaelle.json` fehlte bisher (reines Versehen) in `.gitignore`, obwohl alle Schwesterdateien dort stehen — sofort nachgetragen. Neuer Default **`Path.home() / ".kursplaner" / "schulweite_ausfaelle.json"`** statt eines Pfads unter `SCRIPT_DIR`/`WORKSPACE_ROOT` (Letztere sind für Nutzdaten ungeeignet — `WORKSPACE_ROOT` zeigt nachweislich nicht auf das echte, per `paths.json` konfigurierte Vault dieses Nutzers, und ein `Path.home()`-Default hat kein Henne-Ei-Problem mit einer selbst noch nicht konfigurierten `paths.json`). Pfadauflösung/Migration liegt bewusst in der Config-/Store-Schicht, nicht in der GUI:
  - `core/config/ui_preferences_store.py`: `load_school_wide_cancellations_path_override()`/`save_school_wide_cancellations_path_override(value)` — rein technische String-Ablage, keine Pfadauflösung.
  - `core/config/school_wide_cancellations_store.py`: `_store_path()` löst Override → Default auf; `_migrate_legacy_default_if_needed()` migriert beim ersten Zugriff automatisch die alte `config/`-Datei; `set_store_path_override(new_value)` kapselt den manuellen Wechsel über die Einstellungen. Migration/Wechsel folgen beidemal demselben Muster: **zuerst kopieren (`_copy_verified`, prüft Existenz + Dateigröße der Kopie), erst bei verifiziertem Erfolg die alte Datei löschen** — nie verschieben, nie vor Verifikation löschen. Ist der neue Zielpfad nicht beschreibbar (`_probe_writable`), wird `RuntimeError` geworfen und der Override nicht gespeichert — der bisherige, funktionierende Pfad bleibt aktiv.
  - `adapters/gui/settings_window.py`/`path_settings_controller.py`: neue Settings-Section "Schulweite Ausfälle" (reines String-Feld); GUI kennt keine Pfadauflösung, ruft nur `set_store_path_override(...)` auf und zeigt bei `RuntimeError` einen `messagebox.showerror`, Dialog bleibt offen.
  - Alle bestehenden Konsumenten (`SchoolWideCancellationFlow`, Diagnostics, Overlap-Query, der neue Punkt-C-Usecase) rufen weiterhin ausschließlich `load_school_wide_cancellations()`/`save_school_wide_cancellations()` auf — unverändert gegenüber vorher, keine Pfad-Kenntnis in der Business-Logik.

**Tests**: 562/562 grün (33 neue: `test_school_wide_cancellations_store.py`, `test_school_wide_cancellation_dialog_dates.py`, `test_claims_for_grade.py`, `test_apply_school_wide_cancellations_to_new_rows_usecase.py`). `tools/ci/check_ai_guardrails.py`: bestanden (derselbe vorbestehende, nicht blockierende `open-settings`-Warnhinweis wie zuvor).

### Added (2026-08-16) — Schulweite Ausfälle

Neues Feature: ein Popup aus der Kursübersicht, um einen Ausfall (Grund, Zeitraum, betroffene Jahrgangsstufen) auf viele Kurse gleichzeitig anzuwenden — inkl. automatischer Verschiebung betroffener Einheiten und, falls kein Platz mehr im Plan ist, automatischem Anhängen datumsloser Einheiten (rot umrandet im Grid).

**Bewusst ausgeschlossen**: Das `Leiste`-Feld (`Do 2 07:50` etc.) aus Vault-Markdown-Dateien wird nicht geparst — im gesamten Bestandscode ungenutzt und für dieses Feature nicht erforderlich. Betroffene Einheiten werden ausschließlich aus den vorhandenen Datumszeilen der Kurspläne ermittelt (`plan_row_placement.find_stattfindend_rows_in_range`), dieselbe Selektionslogik für Preview und Apply.

**Ledger-Design — Position statt Content-Snapshot**: `core/domain/school_wide_cancellation.py` modelliert eine verschobene Einheit als `UnitMove(source, reference, target)`, nicht als aggregierte Datumslisten/Zählwerte. Identität = Datum + Position innerhalb des Datums (`RowLocation`) + Markdown-Referenz (`UnitReference`, Link-Ziel oder normalisierter Freitext) — der Inhalt der referenzierten Datei ist bewusst NICHT Teil der Identität. `reference`/`target` sind `None`, wenn eine stornierte Zeile keinen Inhalt trug (nichts zu verschieben). Kein `appended_row_count`, keine „letzte N Zeilen"-Logik: eine datumslose Zielzeile wird bei Revert über Referenz-Abgleich unter allen aktuell datumslosen Zeilen wiedergefunden, nie über Position/Zählung.

**Identitätsauflösung** (`core/domain/row_identity.py::resolve_move`): pure Funktion, löst einen `UnitMove` gegen die *aktuelle* Tabelle auf und liefert `MATCH`/`WARNING`/`ERROR`. `WARNING` nur, wenn Quelle und Position gefunden werden, aber die Referenz an der Zielposition abweicht (struktureller Konflikt, z. B. Zeile wurde inzwischen anders verlinkt) — ein reiner Inhaltswechsel der verlinkten Datei selbst bleibt `MATCH`. Wird sowohl von Revert (um zu handeln) als auch vom Diagnose-Usecase (um nur zu berichten) genutzt — ein Primitiv, keine doppelte Matching-Logik.

**Fehlerphilosophie zentral, nicht dupliziert**: `core/usecases/bulk_cancellation_coordinator.py::BulkCancellationCoordinator` ist die einzige Stelle mit Fehler-/Konflikt-/Rollback-Logik. Apply und Revert selbst (`school_wide_cancellation_{apply,revert}_usecase.py`) sind single-course und entscheidungsfrei. Bei Fehler/Konflikt ruft der Coordinator einen generalisierten Decision-Callback auf (`ConflictDecision`/`ConflictContext`/`ConflictResolution` in `core/ports/repositories.py`, Erweiterung des bestehenden `ConfirmChange`-Patterns um `RETRY | SKIP | ROLLBACK | PROCEED`; `PROCEED` nur bei Warnungen). Rollback = In-Memory-Undo-Stack des aktuellen Vorgangs in umgekehrter Reihenfolge abarbeiten — Apply-Schritte per Revert rückgängig, Revert-Schritte per erneutem Apply mit denselben Parametern rückgängig; reine Wiederverwendung von Apply/Revert, keine dritte Implementierung.

**Keine künstliche Reihenfolgepflicht zwischen Entries** (Top-of-stack-Modell verworfen): ein `(Kurs, Datum)` darf zu jedem Zeitpunkt von höchstens einem aktiven Entry als Ausfall beansprucht sein (`school_wide_cancellation.find_claiming_entry`), geprüft von Preview und Apply. Da Zielzeilen referenzbasiert statt positional identifiziert werden, bleibt Revert eines Entrys unabhängig davon sicher, was andere Entries in derselben Tabelle sonst getan haben.

**`edit()`-Zweiphasigkeit** (`core/flows/school_wide_cancellation_flow.py`): läuft NICHT als ein kombinierter Batch, sondern als zwei `coordinator.run()`-Aufrufe (erst alle bisher getrackten Kurse zurücknehmen, DANACH die Vorschau für die neuen Parameter frisch berechnen und anwenden). Grund, per Test (`test_edit_narrower_range_reverts_dropped_dates`) gefunden: solange ein Kurs noch die alte Ausfall-Markierung desselben Entrys trägt, gilt er für `find_stattfindend_rows_in_range` als nicht-stattfindend und wäre in einer vorab (vor dem Revert) berechneten Vorschau fälschlich als "nicht betroffen" erschienen — genau die Tage, die derselbe Entry weiterhin beanspruchen soll, wären verloren gegangen. Bricht die Apply-Phase ab, werden die in Phase 1 bereits zurückgenommenen Kurse per Kompensations-Apply mit den alten Parametern wiederhergestellt.

**Datumslose Zeilen — explizite Invariante statt implizitem Seiteneffekt**: `core/domain/plan_row_placement.py::strip_empty_dateless_rows` entfernt jede Zeile ohne Datum und ohne Inhalt, unabhängig von ihrer Herkunft (auch vorbestehende). Bewusst NICHT in `save_plan_table` versteckt, sondern explizit aufgerufen am Ende von Schulweite-Ausfall-Apply/-Revert und vom `TimetableChangeDialog`-Übernehmen-Pfad (`ApplyTimetableChangeUseCase.execute`). `plan_gap_placement` (dieselbe Datei) ist die gemeinsame "finde freie Slots, alles andere wird angehängt"-Suchlogik, geteilt zwischen dem neuen Feature und `TimetableChangeDialog._place_displaced_content` (ersetzt dessen bisheriges Verhalten, Inhalt bei fehlendem freien Slot zu verwerfen — jetzt wird analog ein `DraftSlot(datum=None, ...)` angehängt, `DraftSlot.datum` ist jetzt `date | None`).

**Rendering**: neue Methode `DayColumn.is_dateless()` (`core/domain/day_column.py`, Stil wie die übrigen Ableitungsmethoden: `_row_date() is None and bool(inhalt.strip())`). `grid_renderer.py::_apply_ub_border` bekommt einen vorrangigen Zweig (`color_token="danger"`, bereits in jedem Theme vorhanden) vor der bestehenden UB-Prüfung; ein neuer gemeinsamer Helper `_border_thickness()`/`_accent_border_active()` ersetzt die bisher dreifach duplizierte `2 if is_ub else 1`-Berechnung an allen Stellen, die die Rahmenbreite setzen.

**Kleine Konsolidierung nebenbei**: `_extract_primary_link_target`-artige Link-Extraktion war bereits 2× dupliziert (`load_plan_detail_usecase.py`, `query_ub_plan_usecase.py`); für die neue Referenz-Identität wurde eine dritte, gemeinsame Fassung `wiki_links.py::extract_wiki_link_target` ergänzt und beide Bestandsduplikate darauf umgestellt.

**bw-gui-Policy**: `GradeGroupSelector` (`adapters/gui/grade_group_selector.py`) komponiert einen `widgets.Frame` statt ihn zu erben (wie `WeekdayRhythmPicker`), mit `__getattr__`-Delegation an den Container — vermeidet eine neue lokale UI-Basisklasse (`tools/ci/check_ai_guardrails.py`-Guardrail `_check_repo_wide_gui_contracts` hat den ursprünglichen Vererbungs-Versuch zurecht abgelehnt).

**Tests**: 529/529 grün (74 neue: `test_grade_groups.py`, `test_plan_row_placement.py`, `test_row_identity.py`, `test_school_wide_cancellation_{preview,apply,revert,flow,diagnostics}_usecase.py`, `test_bulk_cancellation_coordinator.py`). `tools/ci/check_ai_guardrails.py`: bestanden (ein vorbestehender, nicht blockierender Warnhinweis zu `open-settings`, unverändert).

### Fixed (kritisch, 2026-08-16) — Stundenplanänderung verschluckte den Plan ab `Rhythmus`

Von Nutzer gemeldet anhand einer real beschädigten Datei: nach Übernahme einer Stundenplanänderung endete die Datei direkt nach dem `Rhythmus:`-Block der Frontmatter — schließendes `---`, komplette Plantabelle und alles danach fehlten.

**Ursache**: `plan_repository.py::update_plan_rhythm()` scannt die Datei zeilenweise, um den `Rhythmus:`-Block chirurgisch zu ersetzen (`start_idx`/`end_idx`). Die Schleife brach beim schließenden `---` sofort ab (`break`), **bevor** sie `end_idx` auf dessen Zeilenindex setzen konnte, falls `Rhythmus` das letzte Feld vor `---` war (kein weiteres Feld dazwischen, das `end_idx` schon gesetzt hätte). `end_idx` blieb dadurch `None` und fiel auf den Fallback `len(lines)` zurück — wodurch `lines[end_idx:]` (schließendes `---` + Tabelle + alles danach) beim Zusammenbau der neuen Datei komplett wegfiel. Betroffen war jede Datei, bei der `Rhythmus` das letzte Frontmatter-Feld ist — nach der Migration (Part A/F) der Normalfall, da `Rhythmus` immer zuletzt geschrieben wird und optionale Folgefelder (`Kompetenzen`, `Stundenziel`) meist fehlen.

Fix: beim Erreichen von `---` wird `end_idx` (falls noch offen) zuerst gesetzt, dann erst `break`. Reine Ein-Zeilen-Korrektur der Bedingungsreihenfolge. Neue Tests `tests/test_plan_repository_update_rhythm.py` (Rhythmus als letztes Feld inkl. Tabelle + Freitext-Abschnitt danach bleiben erhalten; Felder nach Rhythmus bleiben unverändert; fehlendes Rhythmus-Feld wirft weiterhin `RuntimeError`) — für diese Methode existierte zuvor kein einziger Test, was den Bug unentdeckt ließ.

**Zusätzlich vom Nutzer gemeldet und mitbehoben**: `apply_timetable_change_usecase.py::execute()` hängte ein neues Rhythmus-Segment über `add_segment()` immer als zusätzliches `ab <Datum> …`-Segment an die bestehenden Einträge an — auch wenn `date_from` faktisch der erste Plantag war (keine frühere Zeile existiert, die das alte Segment noch bräuchte). Ergebnis war ein sinnloses, nie greifendes altes Segment neben einem unnötig datierten neuen. Neue Prüfung `_has_row_before(table, date_from, idx_datum)`: existiert keine Planzeile vor `date_from`, wird der Rhythmus komplett ersetzt statt ergänzt, und die neuen Einträge verlieren ihr `valid_from` (sie gelten dann unconditioned seit Kursbeginn, kein `ab`-Präfix nötig). Tests in `test_apply_timetable_change_usecase.py`: ein Fall mit früherer Zeile (Segment bleibt erhalten, `add_segment`-Pfad), ein Fall ohne (vollständiger Ersatz, `valid_from` entfernt).

455/455 Tests grün, `ruff`/`mypy`/Guardrails ohne neue Findings gegenüber dem Stand vor dieser Änderung.

### Changed (technische Details, 2026-08-15) — `DayColumn`-Objektkapselung (Part K)

Letzte Phase des Rhythmus/Ferien/Archiv-Vorhabens: das bisherige lose `day: dict[str, object]` (bis zu 21 lose String-Keys, teils gespeicherter Rohwert, teils bei jedem Zugriff neu berechneter Wert — für Aufrufer nicht unterscheidbar) wird durch eine echte Domain-Klasse `core/domain/day_column.py::DayColumn` ersetzt. Ablauf laut Supervisor-Vorgabe: erst Callgraph-Analyse aller Leser/Schreiber (Explore-Agent, ~33 Quell- + 15 Testdateien), dann konkreter Modellvorschlag zur Rückfrage, erst danach Umsetzung — alle drei Design-Rückfragen (volle Methodenkapselung statt Minimalvariante, live aus mitgeführter Rhythmus-Referenz statt Precompute-Cache, vollständiges Durchziehen statt Teilmigration) vom Supervisor mit der jeweils gründlicheren Option beantwortet.

**Modell**: `DayColumn` ist ein frozen Dataclass mit rohen Feldern (`row_index`, `datum`, `inhalt`, `thema_ausfall`, `link`, `yaml`, `group_name`, `rhythm`, `hidden_kinds_before`) und ausschließlich parameterlosen Ableitungsmethoden ohne Setter (`is_ferien()`, `stunden()`, `startzeit()`, `is_cancel()`, `is_lzk()`, `is_hospitation()`, `is_unterricht()`, `is_ub()`, `stundentyp()`, `oberthema()`, `plan_oberthema()`, `header_content()`, `content_marker_text()`, `is_valid_unterricht_file()`, `is_link_header()`, `is_unresolved_link()`). Jede Ableitung wird live aus den Rohfeldern berechnet, nie zwischengespeichert — `stunden`/`startzeit`/`is_ferien` sind reine Projektionen aus `rhythm`+`datum` bzw. `thema_ausfall`, keine zweite Zustandsquelle. Einzige Mutation im System: `with_hidden_kinds_before()` (über `dataclasses.replace`) für die Sichtbarkeits-Projektion. Neue geteilte Test-Fabrik `tests/day_column_factory.py::make_day_column(**overrides)` — bewusst ohne synthetische Flag-Parameter (kein `is_cancel=True`); Tests setzen echte Rohfelder (`thema_ausfall="X Grund"` für Ausfall, ein real angelegtes File in `Einheiten/` für LZK/Hospitation, da `is_lzk()`/`is_hospitation()` über `is_valid_unterricht_file()` einen existierenden, in einem verwalteten Verzeichnis liegenden Link voraussetzen).

**Erzeugung**: `load_plan_detail_usecase.py::build_day_columns()` baut die `DayColumn`-Instanzen (einzige I/O-Stelle); `PlanDetailResult.day_columns: list[DayColumn]`.

**Migrierte Konsumenten** (Attributzugriff für Rohfelder, Methodenaufruf für Ableitungen): `topic_sequence_runs.py`, `grid_renderer.py`, `selection_controller.py`, `action_controller.py`, `ui_intent_controller.py`, `row_display_mode_usecase.py`, `save_cell_value_usecase.py`, `column_visibility_projection_usecase.py` (nutzt `with_hidden_kinds_before`), `daily_course_log_usecase.py`, `export_topic_units_pdf_usecase.py`, `sync_topic_sequence_plans_usecase.py`, `lesson_context_controller.py::field_value()` (der zentrale dynamische Feld-Dispatch — jetzt explizite Verzweigung pro Feld statt generischem `getattr`, wie vom Callgraph gefordert), `grid_cell_policy_usecase.py` (bestätigt toter Code, für Konsistenz trotzdem mitgezogen statt gelöscht — Löschen wäre eine größere, hier nicht angefragte Aufräumaktion), `column_reorder_controller.py`, `sequence_field_grid_renderer.py`, `timetable_change_dialog.py`/`timetable_change_usecase.py` (inkl. `column_is_ferien`/`column_is_manual_ausfall`/`column_is_stattfindend`), `toolbar_icon_styler.py`, `action_button_state_usecase.py`, `move_selected_columns_usecase.py`, `cleanup_lzk_expected_horizon_links_usecase.py`, `export_expected_horizon_usecase.py` (dortige lokale `_row_type`/`_row_oberthema`-Duplikate von `DayColumn.stundentyp()`/`.oberthema()` entfernt), `export_lzk_expected_horizon_usecase.py`, `tracked_write_usecase.py`, `editor_controller.py`, `lesson_conversion_controller.py`, `plan_regular_lesson_usecase.py::build_dialog_context()`, `plan_lesson_flow.py`, `main_window.py` (`day_columns`/`raw_day_columns`/`_field_value`), `overview_controller.py::_project_visible_day_columns()`.

**Beim Durchziehen gefundene, bereits vor dieser Umstellung real gewordene Bugs** (der vorherige Zwischenstand — DayColumn-Erzeugung bereits umgestellt, viele Konsumenten noch auf dem alten Dict-Zugriff — hätte beim nächsten produktiven Start dieser Codepfade zu Abstürzen bzw. stillen Fehlfunktionen geführt, unabhängig von dieser Migration selbst):

- `move_selected_columns_usecase.py::find_swap_partner()` rief `day.get("is_cancel", False)` auf einem inzwischen echten `DayColumn` auf → `AttributeError` bei jeder Spalten-Verschiebung/-Auswahl (dieser Pfad wird u. a. von `action_button_state_usecase.compute()` für `can_move_left`/`can_move_right` bei jeder Einzelspalten-Selektion durchlaufen).
- `action_button_state_usecase.py::compute()` und `toolbar_icon_styler.py` prüften defensiv `isinstance(selected_day, dict)`, bevor sie `is_cancel`/`is_lzk`/`is_hospitation`/`link` lasen — mit einem echten `DayColumn` immer `False`, wodurch der komplette Kontext-Button- und Toolbar-Icon-Zustand (LZK/Hospitation/Ausfall-Erkennung, "Fortsetzen"-Icon) still auf einen falschen Default zurückgefallen wäre, ohne Fehlermeldung.
- `tracked_write_usecase.py::capture_current_state()` hatte dieselbe `isinstance(day, dict)`-Guard-Klausel — hätte verlinkte Stunden-Dateien beim Undo/Redo-Tracking (`HistoryUseCase.capture`) still übersprungen.

Alle drei durch direkten Attribut-/Methodenzugriff ersetzt (kein `isinstance`-Guard mehr nötig, `DayColumn` ist der einzige erwartete Typ).

**Verifikation**: 450/450 Tests grün (Ausgangspunkt zu Beginn dieser Phase: 44 fehlgeschlagen/406 bestanden). `ruff check kursplaner tools`: 174 Findings — exakt der Stand vor dieser Änderung (Vergleich per `git stash`), keine neuen. `mypy kursplaner`: 21 Findings gegenüber 32 vorher (die Migration hat nebenbei mehrere vorbestehende dict-typisierte Fehlstellen mit aufgelöst, keine neuen hinzugekommen). `tools/ci/check_ai_guardrails.py`: bestanden (ein vorbestehender, nicht blockierender Warnhinweis zu `open-settings`, unverändert). Kein verbleibender `day.get(`/`day["…"]`/`isinstance(day, dict)`-Zugriff im gesamten `kursplaner/`/`tools/`-Baum (per Grep verifiziert).

### Fixed (technische Details, 2026-08-15) — "Wann"-Zeile bei leeren Einheiten (Part H)

`grid_renderer.py::_field_is_visible_for_day()` hatte eine fest codierte Positivliste `{"inhalt", "stunden", "Oberthema", "Ausfallgrund"}` für Felder, die bei einer noch nicht verlinkten (leeren) Tageszeile trotzdem sichtbar bleiben — geschrieben vor Einführung von `"startzeit"` (Part A) und nie erweitert. Der Wert selbst wurde in `build_day_columns()` bereits unbedingt für jede Zeile berechnet; das Problem lag rein in der Rendering-Sichtbarkeitsprüfung. `"startzeit"` zur Menge ergänzt (Einzeiler). Behebt zwei Symptome: fehlende Zelle in einzelnen unverlinkten Spalten, und das komplette Verschwinden der ganzen "Wann"-Zeile, wenn im Grid gerade keine Spalte verlinkt ist (`_visible_row_defs()` nutzt dieselbe Methode). Test `test_unlinked_day_only_shows_inhalt_stunden_and_oberthema` in `test_row_filter_grid_visibility.py` um `"startzeit"`-Assertion erweitert und umbenannt.

### Fixed (technische Details, 2026-08-15) — Oberthema-Anzeige entschlüsselt (Part I)

Ein Nutzer kann in der YAML einer Unterrichtseinheit `Oberthema:` bewusst als Wiki-Link speichern (Beispiel: `an7sd7.md` hat `Oberthema: "[[11.1 EFl1 Potenzfunktionen]]"`), damit es in Obsidian verlinkt — die YAML-Speicherung bleibt unverändert. Bisher gab der direkte YAML-Lesepfad (`yaml_data.get("Oberthema")`) diesen Rohwert überall unverändert zurück; nur der `plan_oberthema`-Fallback für Zeilen ohne verlinkte Datei war bereits entschlüsselt (via `extract_plan_oberthema`).

**Über reine Kosmetik hinaus gefundener Funktionsbug**: Die Themenfolgen-Erkennung (`topic_sequence_runs.py::compute_topic_sequence_runs`) verglich Oberthema-Werte exakt als String — trug eine Einheit den Klartext und die nächste (gleiches Thema) den Link, riss die Erkennung an der Stelle fälschlich ab. Derselbe Exact-Match-Fehler existierte unabhängig in `export_expected_horizon_usecase.py::_export_rows_for_oberthema` (Kompetenzhorizont-Export filterte Zeilen fälschlich heraus).

**Zentrale Dekodierung statt verstreuter Einzel-Patches**: `core/domain/wiki_links.py` bekommt `strip_group_prefixed_link(raw, group_name)` — die generische "Wiki-Link entfernen + optionalen führenden Gruppennamen-Präfix abschneiden"-Operation, extrahiert aus `plan_table.extract_plan_oberthema` (ruft sie jetzt auf, statt die Logik zu duplizieren). Neuer schmaler Accessor `plan_table.read_yaml_oberthema(yaml_data, group_name)` ist die eine Stelle, die jeder Lesepfad aufruft. `load_plan_detail_usecase.build_day_columns()` stellt dafür jedem `day`-Eintrag zusätzlich `group_name` bereit (bisher nur lokal berechnet, nie mitgeführt).

Umgestellte Lesestellen (nur Lesen/Anzeigen/Vergleichen — Schreibpfade unverändert, die YAML soll den Link behalten): `lesson_context_controller.py::field_value()` (neue Hilfsmethode `_raw_group_name()`, ungesanitized im Gegensatz zu `parse_group_token()`) und sein bytegleiches Duplikat `grid_cell_policy_usecase.py::field_value()` (bestätigt vollständig unbenutzter Code — nirgends in Produktion oder Tests aufgerufen, trotzdem korrigiert statt gelöscht, da das Löschen der ganzen Klasse eine größere, hier nicht angefragte Aufräumaktion wäre); `topic_sequence_runs.py::row_oberthema()` (höchste Priorität, speist Sequenz-Dateiname/-Frontmatter/Export-Untertitel automatisch mit); `lesson_context_query_usecase.py::last_oberthema_before_row()`; `plan_regular_lesson_usecase.py::build_dialog_context()`; `lesson_conversion_controller.py::_unterricht_prefill_values()` (zweite Oberthema-Stelle, die verlinkte-Datei-Override); `export_expected_horizon_usecase.py::_row_oberthema()`/`_export_rows_for_oberthema()`; `daily_course_log_usecase.py::_cells_for_unit()`.

`sequence_planning.py`/`sequence_plan_repository.py`/`export_topic_units_pdf_usecase.py` brauchten keine eigene Änderung — sie konsumieren ausschließlich `run.oberthema`, bereits stromabwärts von `row_oberthema()`.

Tests: `test_wiki_links.py` (`strip_group_prefixed_link`/`read_yaml_oberthema`, Rundtrip, Gruppenpräfix mit/ohne, Klartext unverändert), `test_topic_sequence_runs.py`/`test_export_expected_horizon_usecase.py` je um einen Fall mit gemischter Klartext-/Link-Schreibweise erweitert (muss als eine Sequenz/Export-Gruppe erkannt werden).

### Changed (technische Details, 2026-08-15) — Architektur-Review-Korrekturen (J1–J7)

Ein nachträglicher kritischer Architektur-Review des bereits implementierten Rhythmus/Ferien/Archiv-Features (siehe Eintrag darunter) ergab, dass Split/Merge entgegen der eigentlichen Modellsemantik ("ein Kurstag hat genau eine Einheit") ins neue Rhythmus-Modell integriert statt entfernt worden war. Korrektur (J1, erster Schritt der Review-Nacharbeit):

- **Split/Merge vollständig entfernt**: `core/domain/course_rhythm.py::distribute_hours_over_rows` (verteilte Wochentags-Gesamtstunden auf mehrere Zeilen desselben Datums) ersatzlos gestrichen — stellte technisch funktionierend, aber außerhalb des vorgesehenen Datenmodells, die Möglichkeit mehrerer Einheiten pro Kurstag wieder her. `load_plan_detail_usecase.build_day_columns()` berechnet `stunden`/`startzeit` jetzt direkt aus `hours_for_date`/`start_time_for_date` für das Zeilendatum, ohne Aufteilung auf gruppierte Zeilen.
- `core/usecases/plan_commands_usecase.py`: `split_hour_count`, `split_unit`, `_date_group_row_indices`, `can_merge_date_units`, `merge_units`, `merge_preview`, `MergeResult` sowie die dafür genutzten Hilfsmethoden `_row_date`/`_hours_for_row`/`_row_content` entfernt. Restliche Kommandos (`restore_from_cancel`, `clear_selected_lesson`, `convert_to_ausfall`, `shift_existing_lessons_forward`, `swap_contents`) bleiben unverändert — die Klasse ist ein Mehrzweck-Container, kein Split/Merge-exklusives Modul.
- `core/usecases/split_selected_unit_usecase.py`/`merge_selected_units_usecase.py` gelöscht. `action_button_state_usecase.py` verliert `can_split`/`can_merge` (inkl. der dadurch toten `_to_int`-Hilfsmethode und der `lesson_context_query`-Abhängigkeit, die nur noch für `can_split` gebraucht wurde) sowie die `merge_selected_units`-Abhängigkeit.
- GUI durchgängig bereinigt: `adapters/bootstrap/wiring.py` (Imports/Konstruktion), `action_controller.py` (`split_selected_unit_action`/`merge_selected_units_action` + Feld-Referenzen), `toolbar_viewmodel.py` (Slot-Reihenfolge, Mindestbreiten, `ToolbarActionDefinition`s, Sichtbarkeits-Mapping), `toolbar_icon_styler.py` (Icon-Zuordnung; `assets/toolbar/tb_split.png`/`tb_merge.png` gelöscht), `screen_builder.py` (Breiten-Gruppierung, Kontextmenü-Einträge), `ui_intents.py` (`TOOLBAR_SPLIT`/`TOOLBAR_MERGE`), `ui_intent_controller.py` (Dispatch + Action-Key-Mapping), `resources/shortcuts/shortcut_guide.json` (Strg+T/Strg+M), `help_catalog.py` (Hilfetexte).
- Tests angepasst statt neu geschrieben: `test_action_controller_paste_enablement.py`, `test_shortcut_policy_toolbar.py`, `test_toolbar_viewmodel.py` (Split/Merge-Assertions entfernt), `test_course_rhythm.py` (`test_distribute_hours_over_rows` entfernt). 433 Tests grün, `ruff`/`mypy` ohne neue Findings gegenüber dem Stand vor dieser Änderung.
- CHANGELOG.md: neuer `### Removed`-Eintrag für die entfernten Toolbar-Aktionen.

J2: `tools/migrate_plan_table_schema.py::_derive_rhythm` sammelte bisher je Wochentag alle historischen Nicht-Null-Stundenwerte in einem `Counter` und wählte den häufigsten (bei Gleichstand den größeren) — ein stiller Best-Effort-Fallback für den Fall, dass ein Wochentag über die Kurshistorie hinweg unterschiedliche Stundenwerte trug (z. B. durch eine frühere, nicht dokumentierte Rhythmusänderung). Widerspricht der fachlichen Vorgabe "keine stillschweigende Reparatur unbekannter Zustände" — historische Tabellen kennen noch keine Rhythmus-Segmente, ein Wochentag mit uneinheitlichen Werten ist daher ein echter Fehlerfall, kein zu erratender Normalzustand. Auf `set[int]` je Wochentag umgestellt; mehr als ein Wert löst `ValueError` mit Wochentag + den widersprüchlichen Werten aus, `migrate_plan()` fängt das ab und liefert `"ERROR: ..."` (Batch läuft mit den übrigen Dateien weiter). Bereits migrierte Kurse sind laut Supervisor-Bestätigung unbetroffen (keine bekannten historischen Rhythmuswechsel) — keine Nacharbeit an existierenden Dateien nötig, reine Härtung für künftige Läufe. Neue Tests: `tests/test_migrate_plan_table_schema.py` (konsistenter Rhythmus, widersprüchliche Werte → ERROR + Datei unverändert, Idempotenz, `--dry-run` schreibt nichts).

J3: `content_markers.py::resolve_cancel_state` überschritt die Modulgrenze — das Modul war vorher rein textbasiertes Marker-Parsing (nur `re`-Import), diese eine Funktion nahm aber zusätzlich `lesson_yaml: dict | None` entgegen und kannte `Stundentyp`. Verschoben nach `core/usecases/lesson_context_query_usecase.py::LessonContextQueryUseCase.resolve_cancel_state` (als `@staticmethod`, unverändertes Verhalten/Signatur) — die Klasse kombiniert an anderer Stelle (`last_oberthema_before_row`, `next_lzk_number`) bereits batchgeladene YAML-Daten mit Zeilendaten, dasselbe Muster gilt jetzt auch hier. `content_markers.py` bleibt strikt auf Text-Marker-Funktionen beschränkt; neuer Klassen-Docstring in `lesson_context_query_usecase.py` hält das als Architekturprinzip fest (nicht nur als Einzelfix), damit künftige YAML-bewusste Funktionen von vornherein hierher statt nach `content_markers.py` wandern. Aufrufer (`load_plan_detail_usecase.py`, `plan_overview_query_usecase.py`) auf den neuen Import umgestellt; Tests aus `test_content_markers_ferien.py` nach `test_lesson_context_query_usecase.py` verschoben.

J4: `coerce_lesson_hours(raw, *, default=2)` lieferte an fünf Aufrufstellen (`editor_controller.py::ensure_link_for_day`, `lesson_conversion_controller.py::convert_selected_to_lzk`/`convert_selected_to_hospitation`, `convert_to_lzk_usecase.py::build_dialog_context`, `plan_regular_lesson_usecase.py::build_dialog_context`) einen stillen Default von 2 Stunden, wenn `day["stunden"]`/`stunden_raw` leer oder ungültig war. Analyse: alle fünf Stellen lesen letztlich denselben Wert (`day.get("stunden")`), der seit `Rhythmus` als SSOT nur noch bei einem nicht parsbaren Zeilendatum leer sein kann (gültige Tage haben immer eine abgeleitete Stundenzahl, auch `0` für Ferien-/Nicht-Unterrichtstage — die sind über `text.isdigit()` bereits vom Default-Pfad ausgenommen). Kein Aufrufer hatte einen fachlich legitimen Rohwert-Fall für den Default; `default=2` hätte für eine kaputte Zeile eine erfundene Stundenzahl in eine neu erstellte Stunden-Datei geschrieben, statt den Fehler sichtbar zu machen.

Ersetzt durch `parse_lesson_hours(raw) -> int` (weiterhin eine konsolidierte Stelle für die Parsing-Logik, jetzt aber ohne Default) — wirft `ValueError` bei ungültigem Wert, statt still zu heilen. `DEFAULT_LESSON_HOURS`-Konstante entfernt (keine verbleibenden Aufrufer). Alle fünf Call-Sites umgestellt; keine zusätzliche Fehlerbehandlung an den Aufrufstellen ergänzt (konsistent mit dem bestehenden Muster in diesem Codebase, z. B. `plan_commands_usecase._idx()`, das ebenfalls unbehandelt `RuntimeError` wirft — Tkinters Default-`report_callback_exception` fängt das ab, ohne die App abstürzen zu lassen). Tests: `test_course_rhythm.py` von `test_coerce_lesson_hours_defaults_on_non_digit` auf `test_parse_lesson_hours_parses_valid_digit_string`/`test_parse_lesson_hours_raises_on_invalid_value` umgestellt.

J5: `course_lifecycle.py::last_plan_date` las `row[0]` positionsbasiert statt über `header_map.get("datum")` wie an allen anderen Datums-Zugriffsstellen im Code — im aktuellen 3-Spalten-Schema unschädlich (Datum ist immer Spalte 0), aber inkonsistent. Auf Header-Lookup umgestellt (liefert `None`, falls die Spalte fehlt).

J6: `overview_controller.py::refresh_overview()` löste die Archivierungs-Dateiverschiebung (`ArchiveFormerCoursesUseCase.execute(...)`) bisher als unbenannten Seiteneffekt mitten in der Methode aus — der Name suggeriert reines Rendering. Extrahiert in eine eigene, benannte Methode `sync_course_storage_lifecycle(unterricht_dir)`; `refresh_overview()` ruft sie als expliziten ersten Schritt auf (keine Verhaltensänderung, reine Verantwortungstrennung — Klärung mit Supervisor: die benannte Trennung zählt, nicht zwingend ein Aufruf außerhalb von `refresh_overview()`). Alle neun bestehenden Aufrufstellen von `refresh_overview()` wurden durchgegangen und entsprechen bereits fachlich relevanten Änderungen (Programmstart `main_window.py`, Kurs anlegen über `NewCourseWindow`s `on_success`-Callback, Pfadänderung/-speicherung in `path_settings_controller.py`, manueller Refresh-Button `TOOLBAR_REFRESH`, Undo/Redo in `_refresh_after_history_step`) statt jeder beliebigen UI-Aktion — insbesondere läuft der häufigste Schreibpfad (`lesson_conversion_controller._refresh_after_write`, nach jeder Zell-/Ausfall-/LZK-/Hospitations-Konvertierung) über `_collect_day_columns`/`_refresh_grid_content`, **nicht** über `refresh_overview()`, sodass der Lifecycle-Sync dort korrekt nicht bei jedem einzelnen Zellen-Edit feuert. `docs/ARCHITEKTUR_KERN.md`-Ausnahme-Eintrag für die Datei aktualisiert (Zeilenzahl, Grund).

### Added (technische Details, 2026-08-09) — Rhythmus/Startzeit, Stunden-Spalte entfernt, Ferien-Marker, Kurs-Archiv

Nutzeranfrage: zu jedem Kurs sollte analog zur bestehenden Stunden-Zeile auch eine Startzeit erfassbar/anzeigbar sein (Vorbild: die rein manuelle Obsidian-Eigenschaft `Leiste:`, die vom Code nie gelesen wurde). Daraus entwickelte sich nach Rücksprache ein deutlich größerer Umbau: das neue Startzeit-Feld wird zur einzigen persistenten Quelle für den Wochenrhythmus eines Kurses, wodurch die bisher redundante `Stunden`-Spalte der Plantabelle entfällt; das macht wiederum ein explizites Ferien-Markierungsformat nötig (bisher implizit über `Stunden==0`); dazu kam eine Umbenennung des irreführend benannten Kurs-Erstelldialogs, automatische Kurs-Archivierung und ein kleiner vorgefundener Oberthema-Prefill-Bug.

- **Neues Domain-Modul `core/domain/course_rhythm.py`**: `WeekdayRhythm`-Dataclass (`weekday`, `start_time`, `hours`, `valid_from: date | None`). YAML-Format je Zeile: `["ab" DD-MM-YY] Wochentag HH:MM Stunden` (z. B. `"Mo 12:15 2"`, `"ab 20-04-26 Mo 14:00 1"`), gespeichert als flache String-Liste im neuen Feld `Rhythmus` der Plan-Datei-Frontmatter (`yaml_registry.PLAN_METADATA_SCHEMA`, jetzt required). Flache Liste statt verschachtelter Mapping-Struktur, weil `parse_yaml_frontmatter` ein handgeschriebener Mini-Parser ist, der nur `Key: scalar` und `Key:` + `- item`-Listen kann. Das optionale `ab <Datum>`-Präfix erlaubt mehrere Segmente je Wochentag (z. B. nach einer Stundenplanänderung) — ohne Segmentierung hätte eine Änderung rückwirkend auch die Stunden vergangener Wochen umgeschrieben; `current_segment(entries, on)` löst je Wochentag das zum Datum `on` gültige Segment auf (spätestes `valid_from`, das nicht in der Zukunft liegt). Weitere zentrale Funktionen: `parse_rhythm`/`format_rhythm` (Roundtrip), `hours_for_date`/`start_time_for_date`, `active_weekdays`, `add_segment` (Segment ergänzen, alte Segmente bleiben für vergangene Zeilen erhalten), `distribute_hours_over_rows(total, row_count)` (verteilt Wochentags-Gesamtstunden gleichmäßig auf mehrere Zeilen desselben Datums — Split-Fall, s. u.), `coerce_lesson_hours` (konsolidiert sechs zuvor verstreute Kopien von `int(x) if x.isdigit() else 2`).
- **Stunden-Spalte der Plantabelle entfernt**: Tabelle ist jetzt `Datum | Inhalt | Thema/Ausfall` (3 statt 4 Spalten). `load_plan_detail_usecase.build_day_columns()` ist der zentrale Rewire-Punkt: `day["stunden"]`/`day["startzeit"]` werden nicht mehr aus einer Spalte gelesen, sondern pro Zeile aus dem geparsten `Rhythmus` + dem Wochentag des Zeilendatums berechnet (0/leer für Ferien-Zeilen, sonst `hours_for_date`/`start_time_for_date`; bei mehreren Zeilen desselben Datums — Split — über `distribute_hours_over_rows` aufgeteilt). Die Dict-Keys bleiben bestehen, wodurch alle nachgelagerten Konsumenten (Export, Reststunden-Summe, Tageslog) unverändert weiterlaufen.
  - `core/usecases/plan_commands_usecase.py` (Split/Merge): `split_hour_count`/`split_unit`/`merge_units`/`merge_preview` lesen/schreiben keine Stunden-Spalte mehr — die Stundenzahl einer Datumsgruppe ist durch den Rhythmus fix, unabhängig von der Zeilenanzahl; Löschen der überzähligen Merge-Zeilen gibt dem verbleibenden Keeper automatisch die volle Stundenzahl zurück.
  - Fünf unabhängige `header_map.get("stunden")`-Lookups bereinigt: `lesson_edit_usecase.py` (toter `set_hours_value`-Pfad entfernt, `is_editable` ließ "stunden" ohnehin nie zur Bearbeitung durch), `apply_timetable_change_usecase.py`, `lesson_context_query_usecase.py::selected_row_hours`, `editor_controller.py`, `plan_overview_query_usecase.py` (`remaining_hours`).
  - `save_cell_value_usecase.py`: kompletter `field_key == "stunden"`-Schreibpfad (Confirmation-Plan + Execute-Branch) entfernt, war seit der `is_editable`-Sperre bereits unerreichbar; `SaveCellConfirmationPlan.require_plan_hours_save`/`require_duration_save` mitentfernt (keine Aufrufer mehr).
  - Drei unabhängige "Rhythmus aus Tabellenzeilen ableiten"-Implementierungen (`timetable_change_dialog._infer_day_hours`, `extend_plan_to_next_vacation_usecase._infer_day_hours`, transiente Dialog-Werte) gelöscht — der Rhythmus wird jetzt direkt aus dem persistenten `Rhythmus`-Feld gelesen (`ExtendPlanToNextVacationUseCase` nutzt `current_segment` ab dem Tag nach dem letzten Plandatum).
  - `core/domain/planner.py`: `PlanRow` von `tuple[date, int, str]` auf `tuple[date, str]` verschlankt; `generate_rows`/`create_plan_result` nehmen `weekdays: set[int]`/`rhythm` statt `day_hours: dict[int, int]` entgegen und schreiben keine Stundenzahl mehr.
  - Fünf unabhängige DD-MM-YY-Parser (`timetable_change_usecase`, `apply_timetable_change_usecase`, `extend_plan_to_next_vacation_usecase`, `daily_course_log_usecase`, `timetable_change_dialog`) zu `plan_table.parse_plan_row_date` konsolidiert.
  - `plan_table_file_repository.py` (war mit ~350 Ausführzeilen bereits vor dieser Session über dem 300-Zeilen-Limit, ohne Ausnahme-Eintrag) in reine Markdown-Tabellen-IO (`plan_table_markdown_io.py`, neu: `_split_row`, `_locate_plan_table_block`, `load_last_plan_table`, `save_plan_table`, …) und verbleibende Lesson-YAML-/Link-Operationen gesplittet; re-exportiert alle bisherigen Namen, damit bestehende Importe (`plan_repository.py`, `lesson_repository.py`, `tools/migrate_group_filenames.py`, Tests) unverändert bleiben.
  - `_parse_plan_metadata`/`plan_repository.load_plan_metadata` stringifizierten bisher jeden Metadatenwert (`str(value)`), was das neue listwertige `Rhythmus`-Feld als Python-Listen-Repr-String statt echte Liste zurückgegeben hätte — auf `(value if isinstance(value, list) else str(value))` korrigiert; `PlanTableData.metadata`-Typ entsprechend auf `dict[str, object]` erweitert.
- **Ferien-Marker `X <Grund> X`**: Ferien sind jetzt eine Teilmenge von Ausfall, textuell über ein zusätzliches abschließendes `X`-Token erkennbar (`content_markers.is_ferien_marker`) — ersetzt den bisherigen impliziten `Stunden==0`-Sentinel, der mit der Stunden-Spalte wegfällt. Erkennung tokenisiert den normalisierten Text und verlangt ein *eigenständiges* letztes Token `X`, um Zufallstreffer zu vermeiden (`"X XLAB"` ist kein Ferien-Marker). `content_markers.py` ist die einzige autoritative Stelle: neue Funktionen `build_ferien_marker`, `marker_reason_text`, `resolve_row_ferien_state`, `classify_row_marker`, `resolve_cancel_state`; `build_ausfall_marker` entfernt jetzt zusätzlich ein eventuelles Schluss-X, damit ein zuvor als Ferien markierter Tag beim erneuten Markieren als normaler Ausfall nicht versehentlich `X … X` behält. Konsolidierte Aufrufstellen: `timetable_change_usecase.py` (`column_is_ferien`/`column_is_manual_ausfall`/`_new_row_is_ferien`, vorher 3 unabhängige Implementierungen, teils auf `day`-Dicts, teils auf rohen `PlanRow`-Tupeln), `lesson_conversion_controller.py::_prefill_ausfall_reason_from_content` (reimplementierte bisher die Präfix-Prüfung inline statt `content_markers` zu nutzen — jetzt entfernt), `daily_course_log_usecase.py::_status_for_unit` (3. unabhängiger Klassifizierungspfad, nutzte einen Inhalt-Text-Fallback, der seit der Migration auf `Thema/Ausfall`-Spalte nicht mehr greifen konnte). Der tote `upgrade_legacy_ausfall_marker` (0 Aufrufer im Produktivcode) wanderte ins Migrationsskript, wo er tatsächlich noch gebraucht wird.
  - **Nebenbei behobene, vorgefundene Inkonsistenz**: `load_plan_detail_usecase.py` überschrieb `is_cancel=True` bei YAML-`Stundentyp: Ausfall` auch ohne Text-Marker; `plan_overview_query_usecase.py` (Reststunden-Berechnung) berücksichtigte diesen Override bisher nicht und konnte dadurch von der Detailansicht abweichend zählen. Neue gemeinsame Funktion `content_markers.resolve_cancel_state(headers, row, lesson_yaml)`; `plan_overview_query_usecase.summarize_plan` musste dafür strukturell umgebaut werden (Ausfall-/Reststunden-Berechnung von der ersten Zeilen-Iteration in die zweite, bereits YAML-ladende Iteration verschoben).
  - **Tageslog** (`daily_course_log_usecase.py`): Ferientage tragen weiterhin Status `"ausfall"` (kompatibel zum bestehenden JSON-Format), zusätzlich ein neues Feld `is_ferien` je Einheit.
- **Kurs-Erstelldialog umbenannt**: `NewLessonWindow`/`new_lesson_window.py` → `NewCourseWindow`/`new_course_window.py`, Titel "Neuer Unterricht" → "Neuer Kurs" (legt einen ganzen Kurs an, keine einzelne Stunde). Neues gemeinsames Widget `adapters/gui/weekday_rhythm_picker.py` (`WeekdayRhythmPicker`: Checkbutton + Startzeit-Entry + Stunden-Spinbox je Wochentag) — sowohl `new_course_window.py` als auch `timetable_change_dialog.py` bauten zuvor dieselbe Checkbutton+Spinbox-Struktur unabhängig auf; die Erweiterung um eine Startzeit hätte diese Duplikation verdreifacht. `timetable_change_dialog.py` sammelt beim Übernehmen ein neues, ab dem gewählten Datum gültiges Rhythmus-Segment (`ApplyTimetableChangeUseCase.execute(rhythm_segment=...)` ergänzt es per `add_segment` und persistiert via neuer `PlanRepository.update_plan_rhythm()` — chirurgischer Ersatz nur des `Rhythmus`-Blocks, alle anderen Frontmatter-Felder bleiben unangetastet). Neue Grid-Zeile `("startzeit", "Wann")` in `RowDisplayModeUseCase.COMMON_ROWS`, direkt vor `("stunden", "Wie lange")` — erscheint automatisch in jedem Anzeige-Modus; nicht editierbar (analog zu `stunden`).
- **Kurs-Archiv**: Neues Domain-Modul `core/domain/course_lifecycle.py` mit der vom Nutzer explizit geforderten *einzigen* Definition von "ehemaliger Kurs": `is_former_course(table, today)` (spätestes Plantabellen-Datum liegt vor `today`) — ersetzt die bisherige, andersartige Heuristik `overview_controller._is_former_course` (`has_any_dated_unit ∧ ¬has_upcoming_unit`), die nur eine Anzeige-Filterung ohne physische Verschiebung war. `course_archive_root(unterricht_dir)` zeigt auf `unterricht_dir/-ALT` (**nicht** `_ALT/Kursordner`, wie ursprünglich vom Nutzer beschrieben — der real im Vault vorhandene Ordner heißt `-ALT` mit Bindestrich, Kurse liegen dort direkt ohne weitere Verschachtelung; während der Migration anhand des echten Vault-Inhalts korrigiert).
  - `plan_repository.py::list_plan_markdown_files` scannt jetzt sowohl `unterricht_dir` als auch `unterricht_dir/-ALT` (Cache-Freshness-Entry von `base_mtime_ns: int | None` auf `root_mtime_ns: dict[str, int]` erweitert, damit beide Wurzeln beobachtet werden). `LessonOverviewItem`/`ListLessonsUseCase` bekommen ein `is_archived: bool`-Feld (`course_lifecycle.is_archived_course_path`).
  - Neuer Port `LessonFileRepository.move_directory(source, target)` (verweigert das Zusammenführen zweier Ordner mit `FileExistsError`), neue Use-Case `core/usecases/archive_former_courses_usecase.py::ArchiveFormerCoursesUseCase` — iteriert alle gefundenen Kurse, verschiebt bei Abweichung zwischen `is_former_course` und aktuellem Ort, überspringt den aktuell geöffneten Kurs (`skip_paths`, verhindert Wegschieben der offenen Ansicht) und einen nicht ladbaren/unmigrierten Kurs (wird nie verschoben), invalidiert am Ende einmalig den Plan-Listen-Cache.
  - Trigger: `overview_controller.refresh_overview()` — läuft ohnehin bei jedem Start und nach jedem Schreibvorgang. Bestehender "Ehemalige anzeigen/ausblenden"-Toggle (Toolbar, Strg+Shift+E) bleibt UI-seitig unverändert, `_should_show_course` filtert jetzt aber nach `lesson.is_archived` statt der alten Heuristik.
- **Migrationsskript `tools/migrate_plan_table_schema.py`** (neu, folgt der `tools/migrate_*.py`-Konvention: `--dry-run`, idempotent): bringt einen eigenen, in sich geschlossenen Leser für das alte 4-Spalten-Format mit (produktive `plan_table_file_repository` akzeptiert nach dieser Migration nur noch 3 Spalten + `Rhythmus`-Pflichtfeld). Leitet je Wochentag die häufigste Stundenzahl aus den alten Zeilen ab (Gleichstand → größerer Wert), Startzeit immer `"00:00"` (Nutzerentscheidung — kein Versuch, die alte, rein manuelle `Leiste`-Eigenschaft zu parsen). Rewrite der Marker, solange die alte Stunden-Spalte noch lesbar ist: `Stunden==0` → Ferien-Marker, `Stunden>0` mit bestehendem Ausfall-Marker → auf das kanonische `X <Grund>` normalisiert. `--check` lädt anschließend jede Datei über den neuen strikten Loader gegen. Gegen synthetische Testdaten verifiziert (inkl. Idempotenz, Frontmatter-Feld-Erhalt für nicht betroffene Felder wie `KC-Profil`/`Kompetenzen`) und danach real gegen den Vault gefahren: 10/10 Dateien migriert (6 aktive + 4 archivierte Kurse unter `-ALT`), `--check` bestanden.
- **Oberthema-Prefill-Bugfix** (kleiner, isolierter Fund während der Recherche): `lesson_conversion_controller.py::_unterricht_prefill_values` suchte beim Umwandeln einer leeren Zeile in "Unterricht" das Oberthema ausschließlich rückwärts über bereits verlinkte Vorzeilen (`last_oberthema_before_row`) und ignorierte dabei ein direkt in die aktuelle, noch unverlinkte Zeile eingetragenes Oberthema (verfügbar über `day["plan_oberthema"]`, seit `is_editable()` "Oberthema" schon immer auch ohne Link editierbar macht). Fix: erst der aktuellen Zeile eigener Wert, dann die Rückwärtssuche. Identischer Bug in `plan_regular_lesson_usecase.py::build_dialog_context` im selben Zug mitbehoben (Dedup-Prinzip).
- **Tests**: `test_course_rhythm.py`, `test_content_markers_ferien.py`, `test_course_lifecycle.py`, `test_unterricht_prefill_oberthema.py` (neu). ~15 bestehende Testdateien auf 3-Spalten-Fixtures + `Rhythmus`-Metadaten aktualisiert (u. a. `test_timetable_change_usecase.py`, `test_apply_timetable_change_usecase.py`, `test_daily_course_log_usecase.py`, `test_wiki_links.py`, `test_create_plan_write_modes.py`, `test_plan_overview_with_index.py`, `test_plan_repository_append_rows.py`, `test_save_cell_value_usecase.py`, `test_save_plan_table_stale_file.py`, `test_new_lesson_competencies.py`, `test_new_lesson_form_horizon.py`). Alle 438 Tests grün.

### Added (technische Details, 2026-08-09)

- **Zeilenfelder pro Anzeige-Modus konfigurierbar + neue Ausfallgrund-Zeile** (Nutzeranfrage: der Ausfallgrund war früher Teil der Inhalt-Zeile, ging beim Umbau auf das 4-Spalten-Schema als eigene Sichtbarkeit verloren; zusätzlich wollte der Nutzer die bisherige Ein-Feld-Anzeigen-Checkbox im "Zeilenfelder…"-Dialog durch vier Modus-Checkboxen ersetzt haben):
  - **Datenmodell**: `RowFilterSettings` (`core/usecases/row_display_mode_usecase.py`) von `hidden_fields: frozenset[str]` (global ein/aus) auf `field_mode_overrides: dict[str, frozenset[str]]` umgestellt — ein `field_key` ohne Eintrag behält seine aus `RowDisplayModeDefinition.rows` abgeleitete Standard-Moduszugehörigkeit (`default_modes_for_field()`), ein vorhandener Eintrag ersetzt sie vollständig (`effective_modes_for_field()`) und erlaubt dadurch sowohl Abwählen einzelner Modi als auch Hinzufügen eines Feldes zu einem Modus, in dem es standardmäßig nicht vorkommt.
  - `row_defs_for_mode()`, `field_is_relevant_for_day()`, `is_editable()` bekommen einen optionalen `settings`-Parameter und prüfen Modus-Zugehörigkeit jetzt über `effective_modes_for_field()` statt über die fest verdrahtete `rows`-Liste des jeweiligen `RowDisplayModeDefinition`. Die Sonderregel für `Professionalisierungsschritte`/`Nutzbare Ressourcen` (nur bei Unterricht + vorhandenem UB-Link) bleibt bewusst hart codiert und nicht durch Overrides erweiterbar — beide Felder hängen technisch an UB-Daten, die nur für verlinkte Unterrichtsstunden geladen werden; ein Override auf einen anderen Modus hätte dort ohnehin nie einen Wert geliefert.
  - Aufrufer durchgereicht: `grid_renderer.py` (`_field_is_visible_for_day()`, Zellrendering), `selection_controller.py` (Tastaturnavigation, dort auch die bisher separate `row_filter_settings.is_visible()`-Prüfung entfernt — sie ist jetzt vollständig in `is_editable()`/`field_is_relevant_for_day()` integriert, keine doppelte Filterlogik mehr), `editor_controller.py`, `ui_intent_controller.py` (3 Stellen), `save_cell_value_usecase.py::build_runtime_context()` (neuer `row_filter_settings`-Parameter), `main_window.py` (`self.row_defs` wird jetzt auch bei `_set_row_filter_settings()` neu berechnet, nicht nur bei `_set_row_mode()` — notwendig, da ein Feld durch einen Override jetzt auch im aktiven Modus neu auftauchen kann).
  - `_field_is_visible_for_day()` in `grid_renderer.py`: Die bisherige Kommentar-Garantie ("Zeilenfilter vor Unlinked-Guard prüfen, sonst lässt sich Inhalt bei unverlinkten Tagen nicht ausblenden") wurde in die jetzt modus-bewusste Prüfung übernommen — der Unlinked-Guard-Fallback (`{"inhalt", "stunden", "Oberthema", "Ausfallgrund"}`) läuft weiterhin durch `field_is_relevant_for_day()`, statt sie zu umgehen.
  - **Neue Zeile "Ausfallgrund"**: `AUSFALL_ROWS` um `("Ausfallgrund", "Ausfallgrund")` ergänzt. `lesson_context_controller.py::field_value()` liefert den bereits beim Laden berechneten Grund (`day["header_content"]`, gleiche Quelle wie der Kopfzeilen-Text) nur auf Ausfall-Tagen, sonst leer. Read-only (Nutzerentscheidung): `"Ausfallgrund"` zur nicht-editierbaren Feldmenge in `is_editable()` hinzugefügt — Ändern weiterhin nur über "Als Ausfall markieren" (Markertext `X <Grund>` in der `Thema/Ausfall`-Zelle). In `grid_renderer.py` zusätzlich (wie `Vertretungsmaterial`) von der Ausfall-Grau-Einfärbung (`canceled_visual`) ausgenommen, da es der eigentliche Inhalt der Ausfall-Spalte ist, nicht eine ausgegraute Regelspalte.
  - **RowFilterDialog** (`adapters/gui/row_filter_dialog.py`): vier `Checkbutton`-Spalten (U/L/A/H) statt einer "Anzeigen"-Spalte, vorbelegt aus `effective_modes_for_field()`. Pfeiltasten-Navigation von 1D (hoch/runter) auf 2D (zusätzlich links/rechts zwischen den vier Modus-Spalten) erweitert. `_accept()` nimmt ein Feld nur in `field_mode_overrides` auf, wenn die gewählte Modus-Menge von `default_modes_for_field()` abweicht, um die Persistenzdatei schlank zu halten.
  - **Persistenz** (`core/config/ui_preferences_store.py`): neues JSON-Format `{"field_mode_overrides": {"<field>": ["<mode>", ...]}}`. `load_row_filter_settings()` migriert das alte `{"hidden_fields": [...]}`-Format automatisch (jedes global versteckte Feld → Override auf leere Modus-Menge), damit bestehende Nutzer-Einstellungen (Feature erst vom 2026-08-04) beim ersten Start nach dem Update nicht stillschweigend verloren gehen.
  - Tests: `test_row_filter_settings.py` (Datenmodell + Persistenz-Roundtrip + Migrationstest), `test_row_filter_grid_visibility.py` (u. a. neuer Test, dass ein Feld per Override zusätzlich in einem fremden Modus erscheinen kann, sowie Ausfallgrund-Sichtbarkeit), `test_row_filter_navigation.py`, `test_lesson_context_controller.py` (Ausfallgrund-`field_value()`), `test_main_window_intents.py`/`test_selection_controller.py` (Stub-Signaturen an den neuen `settings`-Parameter angepasst).

### Fixed (technische Details, 2026-08-08)

- **Externe Änderungen vor/nach der Einheitentabelle gingen beim nächsten Speichern aus Kursplaner verloren**: `self.app.current_table` wird beim Auswählen eines Kurses einmalig geladen (`overview_controller.load_selected_table` → `load_last_plan_table`), inklusive `source_lines`/`start_line`/`end_line` als Snapshot des kompletten Dateiinhalts. Jede Speicherung aus der App (`save_plan_table`) schrieb bisher `source_lines[:start_line] + neue_tabelle + source_lines[end_line+1:]` zurück — basierend auf diesem beim Laden eingefrorenen Snapshot, unabhängig davon, ob sich die Datei seitdem extern geändert hatte. Bearbeitete der Nutzer die `.md`-Datei direkt (z. B. in einem externen Editor), während Kursplaner den Kurs schon geladen hatte, überschrieb die nächste beliebige Zell-Speicherung (Inhalt, Stunden, Oberthema, …) die externe Änderung wieder — nicht spezifisch für eine Feld-Art, sondern jede Speicherung war betroffen.
  - Fix: `PlanTableData` bekommt zwei neue Felder `source_mtime_ns`/`source_size` (Datei-Signatur zum Ladezeitpunkt, ausschließlich für `plan_table_file_repository.py` gedacht, siehe Docstring dort). `load_last_plan_table()` setzt sie beim Laden. `save_plan_table()` vergleicht sie vor dem Schreiben billig gegen den aktuellen Datei-Stand (`_file_signature()` – nur `stat()`, kein Volleinlesen); weichen sie ab, liest sie die Datei frisch ein und übernimmt `source_lines`/`start_line`/`end_line`/`had_trailing_newline` neu (per `_locate_plan_table_block()`, aus `load_last_plan_table()` herausgezogen, um die Tabellen-Lokalisierung nicht zu duplizieren) – die im Speicher editierten `rows` bleiben dabei unangetastet, da sie die eigentliche Nutzeraktion sind. Nach dem Schreiben wird die Signatur auf den neuen Datei-Stand aktualisiert, damit die nächste Speicherung nicht fälschlich wieder als "extern geändert" gilt.
  - Kapselung: Nur `plan_table_file_repository.py` kennt das Signatur-Konzept; alle anderen Aufrufer (`FileSystemPlanRepository`, `SaveCellValueUseCase`, GUI-Controller) rufen `save_plan_table(table)` unverändert auf.
  - Tests: `tests/test_save_plan_table_stale_file.py` (neu) – Regressionstest für den gemeldeten Fall (externe Notiz vor der Tabelle bleibt nach Zell-Speicherung erhalten) sowie ein Test, der belegt, dass bei unveränderter Datei kein unnötiges Neu-Einlesen stattfindet (`source_lines`-Objektidentität).
  - `CHANGELOG.md`: Eintrag ergänzt, da nutzersichtbarer Datenverlust betroffen war.

- **Toolbar-Icon-GC-Bug (Tkinter `PhotoImage`-Referenz)**: `toolbar_icon_styler.py::apply_state_overrides()` erzeugte pro Aufruf ein neu eingefärbtes Icon in einer lokalen Variable und rief `button.configure(image=img, ...)` auf, ohne `img` dauerhaft zu referenzieren — Tkinter speichert bei `configure(image=...)` nur den internen Tcl-Bildnamen, sodass das Bild sofort nach GC verschwand, während der Button noch darauf zeigte. Regression aus Commit `3a4f58e` (Principle-D-Migration), die die vorherige Persistenz (`self.app.toolbar_button_images`) ersatzlos entfernt hatte.
  - Fix: neue Hilfsfunktion `bw_gui.theming.retain_icon_override(button, photo)` in `A:\Code\bw-gui\src\bw_gui\theming\_widget_utils.py` — sucht den passenden Eintrag in der bereits bestehenden `_icon_button_registry` (jeder über `icon_button()` erstellte Button ist dort schon erfasst) und hält `photo` dort als reinen GC-Keep-Alive-Slot (`override_photo`), bevor sie `button.configure(image=photo)` aufruft. Bewusst keine neue, separate Cache-Struktur (erwogen: `dict[int, PhotoImage]` keyed by `id(button)`) — hätte doppelte Zustandsverwaltung für dieselben Buttons bedeutet und wäre fehleranfällig gewesen, da eine von Python wiederverwendete `id()` eines zerstörten Widgets auf ein späteres, unabhängiges Objekt matchen könnte.
  - `toolbar_icon_styler.py::apply_state_overrides()` nutzt den neuen Helper an beiden bisherigen `configure(image=...)`-Stellen (Disabled-Färbung, State-Swap-Icons).
  - Betrifft die geteilte `bw_gui`-Lib (auch von blattwerk, namenfit, tools4school, korrektor genutzt) — additive Änderung, keine bestehende Signatur geändert.

- **Feiertags-/Ferien-Kalenderdaten**: Ursprünglicher Verdacht des Nutzers ("Feiertage werden nicht beachtet") per Testlauf gegen die echten ICS-Dateien widerlegt — `load_events_from_ics_files()`/`generate_rows()` markieren Feiertage korrekt, sobald sie im berechneten Datumsbereich liegen; der konkret beobachtete Fall lag an einem zu kurzen erzeugten Zeitraum, kein Code-Defekt. Die vom Nutzer unabhängig davon angeforderte Absicherung wurde trotzdem umgesetzt:
  - `new_lesson_window.py::_run()`: fehlende Kalenderjahre lösen jetzt vor dem Erfolgs-Popup ein eigenes `messagebox.showwarning(...)` mit Downloadhinweis (ferienwiki.de, Dateinamenskonvention) aus, statt nur als Anhängsel im Erfolgstext zu erscheinen.
  - `DEFAULT_CALENDAR_DIR` (zeigte auf den nicht mehr existierenden Ordner `Code/schulhelfer/...`) aus `core/config/settings.py` entfernt. Pfade sind reine lokale Konfiguration, kein Code-Fallback (Nutzervorgabe). `path_store.py::validate_paths()` bekommt einen neuen optionalen `raw_values`-Parameter und meldet einen leeren Rohwert jetzt explizit als "nicht konfiguriert"-`PathIssue`, statt dass ein leerer String über die Pfadauflösung ggf. zufällig auf einen existierenden Ordner (`WORKSPACE_ROOT`) auflöst und die Prüfung fälschlich besteht. Greift automatisch über den bereits vorhandenen Startup-Flow `ensure_paths_interactive()`.
  - `new_lesson_form_usecase.py::_ferien_starts()` lud bisher ein 4-Jahre-Fenster (`{start-1, start, start+1, start+2}`) für die Formular-Vorschau, während `create_plan_usecase.py` für die tatsächliche Planerstellung ein 3-Jahre-Fenster (`{start, start+1, start+2}`) nutzt. Das Jahr `start-1` wurde nachweislich nie ausgewertet (sowohl `find_next_halfyear_boundary_start` als auch der Ergebnisfilter berücksichtigen nur `start_date <= start`) — auf 3 Jahre angeglichen.
  - **Nebenbei entdeckt und mitkonsolidiert**: `core/config/path_store.py` lag mit ~308 ausführbaren Zeilen bereits vor dieser Session über dem 300-Zeilen-Limit, ohne in der Ausnahme-Tabelle (`ARCHITEKTUR_KERN.md` Abschnitt 28) zu stehen. Statt die Datei unreflektiert als weitere Ausnahme einzutragen, wurde geprüft, ob sich Logik von Daten trennen lässt — `PATH_FIELD_DEFINITIONS` (Dataclass `PathFieldDefinition` + Tupel mit Hilfetexten, ~90 Zeilen reine Daten) nach neuem, vollständig datenhaltigem Modul `core/config/path_field_definitions.py` ausgelagert. `path_store.py` re-exportiert alle bisherigen Namen (`*_KEY`-Konstanten, `PathFieldDefinition`, `PATH_FIELD_DEFINITIONS`, `PATH_FIELD_BY_KEY`) unverändert, sodass alle ~20 bestehenden Importstellen (`action_controller.py`, `new_lesson_window.py`, `subject_source_repository.py`, `tools/migrate_*.py`, u. a.) unangetastet bleiben. `path_store.py` liegt jetzt bei ~243 Zeilen, keine Ausnahme mehr nötig.

- **Oberthema ging beim Stundenplanwechsel verloren**: `TimetableChangeUseCase._build_draft_slots()` übertrug beim Umbauen des Stundenplans nur `day["inhalt"]` alter Einheiten; das `Thema/Ausfall`-Zellfeld (Oberthema-Kodierung für noch nicht angelegte Einheiten, siehe `plan_table.py::extract_plan_oberthema`) wurde nirgends mitgenommen. `DraftSlot` hatte dafür kein Feld; `ApplyTimetableChangeUseCase._build_row()` beschrieb für normale Slots nur `idx_inhalt`, sodass die alte `Thema/Ausfall`-Zelle beim Splicen (`_splice_rows`) ersatzlos verloren ging.
  - Erster Entwurf (ein isoliertes `thema_ausfall`-Feld an `DraftSlot`) verworfen: `_build_row()` schreibt die Thema/Ausfall-Spalte bereits für Ferien/Ausfall aus `slot.ausfall_reason` (transformiert über `format_outage_note`/`build_ausfall_marker`) — ein drittes, anders benanntes Feld hätte das Spalten-Mapping unübersichtlicher gemacht statt klarer. Stattdessen `DraftSlot.oberthema_cell: str` mit Docstring, der die vollständige Feld→Spalten-Zuordnung aller drei Felder dokumentiert.
  - Bewusst der **rohe** `day["thema_ausfall"]`-Zellwert übernommen (kein Re-Encoding über `sync_thema_ausfall_to_plan_row`, das ein bereits geladenes `yaml_data` mit `Stundentyp` einer real verlinkten Datei erwartet, die eine noch nicht angelegte Einheit per Definition nicht hat).
  - `timetable_change_usecase.py::_build_draft_slots()`: `pending_contents` von reinen `inhalt`-Strings auf Tupel `(inhalt, thema_ausfall)` umgestellt.
  - `apply_timetable_change_usecase.py::_build_row()`: schreibt `slot.oberthema_cell` zusätzlich in `idx_thema_ausfall`, wenn gesetzt.
  - `timetable_change_dialog.py::_refresh_right_tree()` zeigt ein gesetztes `oberthema_cell` jetzt im Vorschau-Treeview an (`extract_plan_oberthema`, analog zu `strip_wiki_link(slot.content)`) — vorher wäre der Wert für den Nutzer im Dialog unsichtbar geschrieben worden.
  - `_place_displaced_content()`/`_on_remove()`/`_on_swap()` berücksichtigen `oberthema_cell` jetzt konsistent bei "ist Slot leer/verschiebbar"-Prüfungen bzw. tauschen/leeren es zusammen mit `content`, damit die Kopplung (Inhalt+Oberthema stammen von derselben alten Einheit) nicht durch manuelle Dialog-Aktionen auseinandergerissen wird.
  - **Konsolidierung der "ist leer"-Heuristik**: `ColumnVisibilityProjectionUseCase._is_empty_day()` (steuert die "Leer"-Zeilenfilterung im Grid) prüfte bisher nur `inhalt`/`link`/`yaml`-Payload, nicht das Oberthema — nutzt jetzt zusätzlich die bereits existierende, an anderer Stelle mehrfach genutzte `row_oberthema()` (`core/domain/topic_sequence_runs.py`). Geprüft und bewusst **nicht** zu einer gemeinsamen Funktion mit den `DraftSlot`-Checks zusammengezogen (andere Datenform: `day`-Dict vs. `DraftSlot`, keine echte Dopplung). `ApplyTimetableChangeUseCase._collect_old_contents()` (Basis der "Schatteneinheiten"-Warnung) bewusst unverändert gelassen, da sie verwaiste verlinkte Dateien meldet, kein allgemeiner Leer-Check ist.
  - Tests: `tests/test_timetable_change_usecase.py::test_compute_carries_oberthema_of_not_yet_created_unit` (neu), `tests/test_apply_timetable_change_usecase.py::test_stattfindend_slot_with_oberthema_writes_thema_ausfall_cell` (neu), `tests/test_column_visibility_projection_usecase.py` (Helper um `plan_oberthema`-Parameter erweitert).

- **Vorbestehende Testausfälle behoben (auf Nutzeranfrage nach Meldung, separat von den drei Bugfixes)**: `tests/test_selection_controller.py` (4 Tests) und `tests/test_load_last_ub_insights_usecase.py::test_execute_ignores_same_day_ub_before_configured_cutoff` schlugen bereits auf `main` fehl.
  - `_SelectionAppStub` fehlten `grid_renderer` (`_apply_cell_selection_style`) und `_is_rebuilding_grid`, seit die Fast-Path-Erweiterung von Commit `4555a8f` (2026-08-04, Grid-Navigation) diese in `set_selected_cell()` liest; `_ActionControllerSpy` fehlte zusätzlich `schedule_action_controls_update()`. Reine Stub/Produktionscode-Drift, kein echter Bug.
  - Die beiden Cutoff-Tests in `test_load_last_ub_insights_usecase.py` nutzten noch das alte UB-Dateinamensformat (`"UB yy-mm-dd Titel.md"`), das seit `UbStem` (Commit `08c0d32`, "UB-Dateinamen vereinfacht") nicht mehr geparst wird — `parse_ub_date_from_stem` lieferte `None`, wodurch der Cutoff-Filter in `_latest_for_domain`/`_latest_sections` komplett übersprungen wurde (Guard `if ub_date is not None and not ...`). Der "nach Cutoff"-Test bestand dadurch nur zufällig, der "vor Cutoff"-Test schlug sichtbar fehl. Auf das neue Format (`"ub yy-mm-dd.md"`) umgestellt.
  - Beide Fixture-Lücken sind reine Testdrift, keine Produktionsbugs — Produktionscode unverändert.

- **Strg+Z reagierte nach Zell-Commit per Fremdklick nicht mehr, obwohl der Button ging**: `ui_intent_controller.py::intent_global_click_commit_cell()` (global an `<Button-1>` gebunden) committet eine aktive Zellbearbeitung beim Klick auf ein beliebiges anderes Widget, rief dafür aber unbedingt `_leave_edit_mode_to_cell(set_grid_focus=False)` auf — der Tk-Tastaturfokus blieb dadurch auf dem committeten (nicht mehr editierten) Zell-`ui.Text`-Widget hängen, sobald das Klickziel selbst nicht fokussierbar genug war (z. B. ein `ttk.Button`). Der generische Shortcut-Pfad (`screen_builder.py::_build_shortcut_handler`) blockte Strg+Z danach fälschlich als "Fokus in Textfeld" (`allow_when_text_input=False`, bewusst so für echtes Zell-Editieren). Der Toolbar-Button war nicht betroffen, da sein `command`-Callback `_emit_intent` direkt ohne Fokus-Prüfung aufruft — eine Divergenz von Button- und Shortcut-Ausführbarkeitsquelle, die Architektur-Abschnitt 5 (Adapter) eigentlich ausschließt.
  - Fix: `set_grid_focus` wird jetzt aus `not self._is_editable_widget(clicked_widget)` abgeleitet — Klick auf ein anderes Eingabefeld lässt dessen eigene Fokussierung unangetastet (unverändertes Verhalten), Klick auf alles andere holt den Fokus zurück aufs Grid.
  - Test: `tests/test_ui_intent_controller_global_click_commit.py` (neu, 2 Tests: nicht-editierbares Klickziel setzt Grid-Fokus zurück, editierbares Klickziel lässt Fokus unangetastet — Regressionsschutz für beide Richtungen).

- **Escape schloss mehrere Popups nicht bei fokussiertem Textfeld, und leakte bei einem simplen Textprompt-Popup zusätzlich ins Hauptfenster**: Zwei getrennte Ursachen, beide an der Wurzel (nicht nur an den gemeldeten Stellen) behoben, da beide in geteilter Infrastruktur lagen, die von mehreren Stellen/Projekten dupliziert bzw. mitgenutzt wird:
  - **bw-gui** (`A:\Code\bw-gui\src\bw_gui\dialogs\scrollable_popup.py::ScrollablePopupWindow._handle_escape_request()`): rief bei fokussiertem Entry/Text/Spinbox/Combobox nur `self.focus_force()` auf das ohnehin schon fokussierte Popup-Fenster auf — nachweislich (per echtem Tk-Testlauf verifiziert) wirkungslos, kein Widget-Fokuswechsel, Escape schien beim Tippen nichts zu tun. Vereinfacht auf ein unbedingtes `return self._request_close()`. Kursplaners eigene `ScrollablePopupWindow`-Subklasse (`kursplaner/adapters/gui/popup_window.py`, Basis für praktisch alle Kursplaner-Dialoge inkl. `LessonBuilderDialog`/Unterrichtseinheitcreator) hatte dieselbe Logik lokal dupliziert ("Keep class-hook resolution ... for test/runtime parity") — Override komplett entfernt, erbt jetzt direkt das reparierte Basisverhalten. Zwei weitere Kursplaner-Dialoge (`LessonKompetenzenSelectionDialog`, `LessonStundenzielSelectionDialog` in `lesson_builder_dialog.py`) hatten das Problem bereits einzeln über eigene `_on_escape`-Overrides umgangen — diese Overrides sind jetzt ebenfalls redundant und wurden entfernt (inkl. der zugehörigen `self.bind("<Escape>", ...)`-Aufrufe), statt als totes Duplikat liegen zu bleiben.
  - **bw-gui** (`A:\Code\bw-gui\src\bw_gui\dialogs\service.py::TextPromptDialogService.askstring()`): rief den nackten `tkinter.simpledialog.askstring()` auf; dessen `Dialog.cancel()` gibt `None` statt `"break"` zurück, wodurch ein Escape-Tastendruck im Popup nicht dort stoppte, sondern bis zum globalen `bind_all("<Escape>")`-Handler der aufrufenden App weiterlief (in Kursplaner: normale Zell→Spalte→Kurs-Eskalation auf dem Hauptfenster, bzw. bei einem im Stundenplanänderung-Dialog verschachtelten Aufruf sogar Schließen dieses übergeordneten Dialogs). Neue Klasse `_EscapeSafeQueryString(tkinter.simpledialog._QueryString)` überschreibt nur `cancel()` um zusätzlich `"break"` zurückzugeben; alle bisherigen Kwargs (`initialvalue`, `parent`, …) bleiben unverändert unterstützt. Betrifft alle 7 `simpledialog.askstring`-Aufrufstellen in Kursplaner (u. a. "Einheit umbenennen", Ausfallgrund-Eingabe) sowie alle anderen bw-gui-Projekte (blattwerk, korrektor, tools4school, namenfit).
  - `docs/ARCHITEKTUR_KERN.md` Abschnitt 28: `lesson_builder_dialog.py` (~785 Zeilen, lag schon vorher über dem Limit) als neue Ausnahme nachgetragen; `ui_intent_controller.py`-Zeilenzahl aktualisiert.
  - Nicht gefixt: Escape tat in der UB-Übersicht ("UB-Ansicht", Strg+Shift+U) ebenfalls nichts, obwohl Code-Analyse und mehrere echte Tk-Testläufe (auch mit der echten `ScrollablePopupWindow`-Klasse) dort ein korrekt funktionierendes Escape-Handling zeigen. Kein Kursplaner-seitiges Pflaster eingebaut (Nutzerentscheidung); wird nach diesem Fix live nachgetestet, ob sich das Problem mit der bw-gui-Basisklassen-Reparatur miterledigt hat.
  - Tests: `bw-gui/tests/test_scrollable_popup.py`, `bw-gui/tests/test_dialog_services.py` (neuer Test für `_EscapeSafeQueryString.cancel()`, alter Test auf neue Implementierung umgestellt), `tests/test_popup_window_escape_focus.py` (Kursplaner) angepasst; `tests/test_lesson_selection_dialog_escape.py` entfernt (testete ausschließlich die jetzt entfernten `_on_escape`-Overrides).

### Fixed (technische Details, 2026-08-07)

- **6 vorbestehende, unabhängige Lint-/Architektur-Befunde behoben** (aufgefallen bei der Verifikation des safe_read-Refactorings vom Vortag, jeweils eigener Commit):
  - Ruff I001: unsortierter Import-Block in `plan_table_file_repository.py` (zwei separate `lesson_directory`-Imports zusammengeführt).
  - mypy: `expected_horizon_markdown_renderer.py::_merge_with_existing()` nutzte `dict[str, object]` für heterogene Zeilen-Einträge, wodurch jeder Feldzugriff auf `object` widened wurde (4 Fehler). Neues `TypedDict` `_PlannedRow` behält die Subscript-Syntax bei.
  - mypy: fehlende Typannotation für `initial` in `plan_table_file_repository.py` ergänzt (`dict[str, object]`, passend zu `LessonYamlData.data`).
  - mypy: `subject_source_repository._subject_roots()` – `fachinhalte_root`/`fachdidaktik_root` explizit als `Path | None` annotiert. Kein Laufzeit-Bug (beide Aufrufer behandeln `None` bereits korrekt), reines Typinferenz-Artefakt, das nur im Full-Package-mypy-Lauf sichtbar wird, nicht pro Einzeldatei.
  - **`tools/ci/check_infra_imports.py`-Layering-Verstöße behoben (3 Dateien)**: `timetable_change_usecase.py` typisierte `calendar_repo` auf die konkrete Infra-Klasse statt auf das bereits passende `CalendarRepository`-Port-Protocol (reiner Typ-Fix). `save_cell_value_usecase.py` importierte die freie Funktion `sync_thema_ausfall_to_plan_row` direkt aus `infrastructure` – neue Methode gleichen Namens auf `PlanRepository`-Port/`FileSystemPlanRepository` (reiner Delegat, analog zum bestehenden `save_plan_table`-Muster). `archive_past_lesson_files_usecase.py` importierte `get_row_link_path` direkt – Konstruktor-Injection von `LessonRepository` nachgerüstet (Port-Methode `resolve_row_link_path` existierte bereits), `wiring.py`/`GuiDependencies` um ein Feld erweitert, `main_window.py` nutzt jetzt `dependencies.archive_past_lesson_files_usecase` statt Selbst-Instanziierung. `tools/ci/check_infra_imports.py` liefert wieder exit 0.
  - **`tools/ci/check_ai_guardrails.py`-Drift korrigiert**: `_check_shared_ui_contract_hardening()` verlangte seit Commit `73d4f8f` `self._shared_menu_bar = SharedCustomMenuBar(...)` in `screen_builder.py` – dieser Code wurde jedoch am 26.07. in Commit `75db01d` bewusst als toter Code entfernt (`KursplanerApp` liefert Menüdefinitionen seitdem über `build_menu()`/`section_spec` an `BwBaseWindow`, das `CustomMenuBar` intern selbst aufbaut), ohne das CI-Gate anzupassen. Seit Juli prüfte das Gate also eine absichtlich nicht mehr existierende Stelle. Korrektur: Die Prüfung verifiziert jetzt `main_window.py` (`class KursplanerApp(BwBaseWindow)`, `def build_menu(self)`, `from bw_gui.menu import section_spec`) statt der entfernten Stelle in `screen_builder.py`; zusätzlich `_forbid_substring` ergänzt, das eine künftige zweite, mit `BwBaseWindow` konkurrierende Menü-Instanz in `screen_builder.py` verhindert.
  - Kein `CHANGELOG.md`-Eintrag, da alle 6 Punkte rein interne Tooling-/Architektur-Korrekturen ohne Nutzersicht sind.

### Fixed (technische Details, 2026-08-06)

- **Duplizierte `except Exception:`-Fehlerbehandlung beim Datei-Lesen im Repository-Layer konsolidiert**: `except Exception: return <default>` rund um `read_text()`/`json.loads()` war unabhängig an 8 Stellen in 8 Dateien dupliziert (`file_relation_registry_repository.py`, `lesson_index_repository.py` (2x), `plan_table_file_repository.py`, `subject_source_repository.py`, `expected_horizon_markdown_renderer.py`, `app_state_store.py`, `ui_preferences_store.py`, `path_store.py`) — jeweils leicht anders geformt, ohne gemeinsame Basis. Neues Modul `bw_libs/safe_read.py` (Read-Pendant zu den bestehenden `atomic_write_text`/`atomic_write_json`-Helfern in `bw_libs/app_paths.py`) bündelt das Muster in einer einzigen Stelle: `read_or_default(read_fn, default)` ist der generische Kern (ersetzt alle bisherigen `except Exception:`-Blöcke), `read_text_or_default`/`read_json_or_default` sind Convenience-Wrapper für die häufigsten Fälle. Zusätzlich neue typisierte Exception-Hierarchie (`RepositoryReadError` → `FileReadError`, `FileParseError`) für `read_text()`/`read_json()`, falls zukünftige Aufrufer zwischen "Datei fehlt" und "Datei ist korrupt" unterscheiden wollen — aktuell nutzt das niemand, aber die default-liefernden Wrapper bauen darauf auf.
  - **Bugfix nebenbei**: `subject_source_repository._write_manifest()` verschluckte bisher auch Schreibfehler von `atomic_write_json()` (widersprach der sonst überall geltenden Konvention "Writes propagieren Fehler", die restlichen Write-Pfade im Projekt fangen nie ab). Der try/except wurde entfernt; Schreibfehler propagieren jetzt wie überall sonst.
  - Bewusst nicht angefasst: `path_store._contains_markdown_files()` (nutzt `rglob`+`StopIteration`, kein Text/JSON-Read, strukturell anderes Muster), `adapters/gui/action_controller.py:273-278` (GUI liest eine Datei direkt statt über ein Repository — eigener Layering-Verstoß, kein Duplikations-Bug), sowie die ca. 40 übrigen `except Exception`-Stellen in `adapters/gui/*.py`/`core/usecases/*.py` (legitimes anderes Muster: Exception-Boundary an der GUI, übersetzt Use-Case-Fehler in `messagebox.showerror`).
  - Tests: `tests/test_safe_read.py` (neu, deckt `read_text`/`read_json`/`read_or_default`/Convenience-Wrapper ab), `tests/test_subject_source_repository.py` (neu, Regressionstest für den Write-Bugfix).
- **Einheiten blieben nach Löschen/Ändern ihres Oberthemas in der Sequenz-Export-Tabelle stehen**: `SyncTopicSequencePlansUseCase.execute()` iterierte bisher ausschließlich über die *aktuell* erkannten Läufe (`compute_topic_sequence_runs()`) und übersprang jeden mit `member_count < MIN_SEQUENCE_MEMBER_COUNT` — verschwand der Oberthema-Text einer Einheit (gelöscht oder geändert) und fiel der zugehörige Lauf dadurch weg oder unter die Mindestgröße, wurde die zugehörige Sequenzdatei nie wieder besucht, ihre `## Export`-Tabelle blieb veraltet. Die Verknüpfung Einheit↔Sequenzdatei ist reiner Text-Abgleich (kein persistentes Link-Feld), sodass sich "die alte Datei" nach dem Löschen des Texts nicht mehr aus dem aktuellen Zustand rekonstruieren lässt.
  - Lösung ist bewusst zustandslos statt gecacht: `SyncTopicSequencePlansUseCase._prune_stale_sequence_documents()` prüft bei jedem Durchlauf die *tatsächlich* im `Sequenzen/`-Ordner vorhandenen Dateien (`FileSystemSequencePlanRepository.list_sequence_documents()`) gegen die aktuell qualifizierenden Oberthema-Texte — kein gemerkter "letzter Zustand", der von der Realität abweichen könnte. Jede Datei, deren `Sequenzname`-Frontmatter (`read_sequence_name()`) nicht mehr unter den aktuell qualifizierenden Läufen ist, wird geleert (`SyncSequenceExportTableUseCase.execute(..., rows=[])`) und via `delete_if_trivial()` gelöscht, falls sie danach auch sonst keinen Inhalt mehr trägt.
  - Deckt jede Änderungsart automatisch ab (Zell-Edit, Merge, Split, Ausfall-Konvertierung, Undo/Redo, Stundenplanänderung, …), da praktisch jede schreibende Aktion vor dem nächsten Grid-Rebuild durch `overview_controller.collect_day_columns()` läuft (direkt oder über `action_controller._refresh_after_write()`), ohne dass ein einzelner Aufrufer etwas von der Bereinigung wissen muss.
  - `FileSystemSequencePlanRepository`: neue Methoden `list_sequence_documents()`, `read_sequence_name()` (wirft wie die übrigen Lesemethoden dieser Klasse bei kaputtem Frontmatter durch, kein abweichendes Fehlerverhalten), `is_trivial()` (kein Brainstorming, kein Sequenzziel/Leitkompetenz, keine Export-Zeilen), `delete_if_trivial()`. Alle vier zusätzlich im `SequencePlanRepository`-Protocol deklariert.
  - **Konsolidierung**: Die "welche Sequenz-Dateien gehören zu diesem Kursplan"-Enumeration existierte bereits unabhängig als private `RebuildFileRelationRegistryUseCase._sequence_paths_for_plan()` (anderer Zweck: manuelles "Index neu aufbauen", kennt nur Dateiexistenz, keine Oberthema-Mitgliedschaft). Glob-Logik nach `core/domain/sequence_planning.py::list_sequence_document_paths()` extrahiert, von beiden Stellen genutzt statt dupliziert.
  - Tests: `tests/test_sync_topic_sequence_plans_usecase.py` (3 neue: Bereinigung bei Schrumpfen unter Mindestgröße mit erhaltener nicht-trivialer Datei, komplette Löschung bei voller Leere, Nicht-Anfassen weiterhin qualifizierender Läufe), `tests/test_sequence_plan_repository.py` (8 neue: Enumeration, Namens-Lesen, Trivialitäts-Fälle, Lösch-Verhalten).
- **Sequenzziel-/Leitkompetenz-Zeile wurde durch ausgeblendete Spaltenarten unterbrochen**: `SequenceFieldGridRenderer._render_one_row()` gruppierte die Tk-Grid-Spalten eines Sequenzlaufs bisher über `compute_contiguous_spans()` (aus dem Geschwisterprojekt `bw_gui`), das nur numerisch lückenlose Spaltennummern zusammenfasst — eine Marker-Spalte (ausgeblendete Spaltenart über "Spaltenarten…") zwischen zwei Tagen desselben Laufs erzeugte dort eine Lücke und zerriss die Zeile in zwei Zellen. Neue Methode `_run_span()` (staticmethod, pure) nutzt stattdessen direkt die Invariante, dass `grid_col` in `GridRenderer._rebuild_grid()` streng monoton in `row_index`-Reihenfolge vergeben wird: für eine zusammenhängende `row_index`-Spanne deckt `[min(grid_cols), max(grid_cols)]` immer die volle Spanne ab, Marker-Spalten und übersprungene Ausfall-Zeilen eingeschlossen — kein Fall für `compute_contiguous_spans()` (das bleibt unverändert, andere Konsumenten in `bw_gui` möglich). Tests: `tests/test_sequence_field_grid_renderer.py` (4 Tests: Marker-Lücke, Ausfall-Lücke, Einzelspalte, keine sichtbare Spalte).
- **"Inhalt" ließ sich bei Einheiten ohne verlinkte Stunden-Datei nicht über "Zeilenfelder…" ausblenden**: `GridRenderer._field_is_visible_for_day()` prüfte für unverlinkte Tage nur `field_key in {"inhalt", "stunden"}` und überging dabei `row_filter_settings` komplett — der Zeilenfilter griff also nur für verlinkte Tage. Reihenfolge korrigiert: Zeilenfilter wird jetzt zuerst geprüft, universell für verlinkte wie unverlinkte Tage; der Unlinked-Guard schränkt danach nur noch zusätzlich ein.
- **Oberthema bei leeren (unverlinkten) Einheiten war weder sichtbar noch editierbar** im normalen Grid (nur die Sequenz-Kopfzeile nutzte den vorhandenen `plan_oberthema`-Fallback). Jetzt: `_field_is_visible_for_day()` erlaubt `"Oberthema"` zusätzlich zu `"inhalt"`/`"stunden"` für unverlinkte Tage; `RowDisplayModeUseCase.is_editable()` erlaubt Oberthema-Bearbeitung auch ohne Link; `MainWindowLessonContextController.field_value()` fällt für Oberthema jetzt auf `day["plan_oberthema"]` zurück, wenn `yaml["Oberthema"]` leer ist; `SaveCellValueUseCase.build_runtime_context()`/`execute()` haben einen neuen Zweig für `field_key == "Oberthema"` ohne `lesson_path`, der den Wert direkt über `sync_thema_ausfall_to_plan_row()` in die `Thema/Ausfall`-Zelle der Plantabelle schreibt (kein Datei-Anlegen nötig); `MainWindowEditorController.apply_value()`-Guard entsprechend erweitert.
- **Konsolidierung**: Die "hat diese Spalte eine verlinkte Stunden-Datei?"-Prüfung war unabhängig dreifach implementiert (`GridRenderer._is_linked_day`, inline in `RowDisplayModeUseCase.is_editable`, inline als `has_known_lesson` in `SaveCellValueUseCase.build_runtime_context`) — eine der Kopien war die Ursache des ersten Bugs oben. Jetzt einzige Quelle der Wahrheit: `RowDisplayModeUseCase.is_linked_day()` (staticmethod), von allen drei Stellen genutzt. `GridRenderer._is_linked_day` entfernt.
- Tests: `tests/test_row_filter_grid_visibility.py` — `test_unlinked_day_ignores_row_filter` umbenannt zu `test_unlinked_day_respects_row_filter` (Assertion gedreht, testete vorher explizit das jetzt korrigierte Fehlverhalten), `test_unlinked_day_only_shows_inhalt_and_stunden` umbenannt/erweitert um Oberthema-Assertion, 2 neue Tests für `is_editable("Oberthema", …)` (unverlinkter Unterricht-Tag vs. unverlinkter Ausfall-Tag).
- `docs/ARCHITEKTUR_KERN.md` Abschnitt 28: `grid_renderer.py`-Zeilenzahl aktualisiert (Entfernen von `_is_linked_day`), neue Ausnahme für `save_cell_value_usecase.py` (~346 Zeilen, lag mit ~327 schon vorher über dem Limit, hier durch die Oberthema-Zweige weiter erhöht) dokumentiert.
- Nicht behoben, unabhängig von diesen Änderungen: `tests/test_load_last_ub_insights_usecase.py::test_execute_ignores_same_day_ub_before_configured_cutoff` schlägt bereits auf unverändertem `main` fehl (per `git stash`-Vergleich verifiziert) — zusätzlich zu den bereits dokumentierten 4 `test_selection_controller.py`-Fehlschlägen (siehe unten).
- **Strg+Enter verließ den Bearbeitungsmodus einer Grid-Zelle nicht**: `screen_builder.py::_bind_shortcuts()` registrierte `<Control-Return>`/`<Control-KP_Enter>` nur für `modes=(UI_MODE_PREVIEW,)`. Fokussiert eine Zelle (ein `ui.Text`-Widget), berechnet `_build_runtime_context()` aber `active_mode = UI_MODE_EDITOR` (Fokus-Text-Check hat Vorrang vor `is_detail_view`) — `evaluate_runtime()` lehnte den Shortcut dadurch allein wegen des Mode-Mismatches ab, noch bevor das gesetzte `allow_when_text_input=True` geprüft wurde; `_on_ctrl_enter()` (korrekt implementiert) wurde nie erreicht. Fix: `UI_MODE_EDITOR` zu `modes` beider Bindings hinzugefügt. Test: `tests/test_screen_builder_ctrl_enter.py::test_ctrl_enter_binding_is_active_while_editing_a_cell`.
- **Escape wurde beim Laden eines Kurses verschluckt**: `overview_controller.py::_show_loading_dialog()` erzeugte ein rohes `ui.Toplevel(self.app)` für den Lade-Hinweis, ohne es über `_track_popup_window()` zu registrieren. `ScreenBuilder._sync_popup_sessions_from_windows()` erkannte es dadurch nur generisch als sichtbares Toplevel-Kind des Hauptfensters und klassifizierte es automatisch als blockierendes `dialog.modal` — `_on_escape()` versuchte es daraufhin über `ScrollablePopupWindow.close_active_popup()` zu schließen, das aber nur echte `ScrollablePopupWindow`-Instanzen kennt (No-Op); jeder Escape-Tastendruck während des Ladens wurde so mit `"break"` verschluckt, ohne dass die Zelle→Spalte→Kurs-Eskalation je erreicht wurde. Fix: `_show_loading_dialog()` registriert das Fenster jetzt direkt nach Erzeugung via `self.app.screen_builder._track_popup_window(dialog, policy_id="dialog.non_blocking")` (analog zum bereits korrekten Debug-Fenster in `screen_builder.py`). Test: `tests/test_overview_controller_loading_dialog.py::test_loading_dialog_is_tracked_as_non_blocking`.
- **Aufräumen**: `ui_intent_controller.py::intent_escape()` berechnete `has_inline_editor` bisher als `detail_active or isinstance(focused, ui.Text)` — praktisch identisch mit `has_parent_state = detail_active`, wodurch `HsmContract.resolve_escape_action()` nie zwischen "Zelle wird gerade editiert" und "Detailansicht ist nur offen" unterscheiden konnte (die tatsächliche Verzweigung lief ohnehin direkt über `ui_state.selection_level`, daher kein sichtbarer Bug). `has_inline_editor` präzisiert auf `ui_state.selection_level == SELECTION_LEVEL_EDIT or isinstance(focused, ui.Text)`, sodass der HSM-Kontrakt-Aufruf jetzt tatsächlich das misst, was sein Name verspricht. Verhalten unverändert.
- **Neuer Guard-Test** gegen ein Wiederauftreten der Escape-Swallow-Klasse: `tests/test_popup_tracking_guard.py::test_every_main_window_toplevel_is_tracked_as_a_popup` scannt `kursplaner/adapters/gui/*.py` per `ast` und schlägt fehl, sobald eine Funktion `ui.Toplevel(self.app)` erzeugt, ohne im selben Funktionskörper `_track_popup_window(...)` aufzurufen.
- `docs/ARCHITEKTUR_KERN.md` Abschnitt 28: `screen_builder.py` (~969 Zeilen) und `ui_intent_controller.py` (~834 Zeilen) waren bereits vor dieser Session über dem 300-Zeilen-Limit, standen aber noch nicht in der Ausnahme-Tabelle — nachgetragen mit Begründung.

### Changed

- **UB-Dateinamen-Format vereinfacht** (2026-08-06): Unterrichtsbesuch-Dateien verwenden jetzt das Format ``ub yy-mm-dd`` (ohne Titel, Kleinbuchstaben). Motivation: Titeländerungen einer Unterrichtseinheit führten bisher zur Namens-Drift zwischen dem Einheitenstamm und dem zugehörigen UB-Stamm.
  - `kursplaner/core/domain/unterrichtsbesuch_policy.py`: Neuer `UbStem`-Dataclass als Value Object (Single Source of Truth für das UB-Stemformat). `build_ub_stem(date_text)` ist nun einparametrig (ohne Titel). `parse_ub_date_from_stem` delegiert an `UbStem.parse()` und akzeptiert nur noch das neue Kleinbuchstaben-Format.
  - `kursplaner/core/usecases/mark_unit_as_ub_usecase.py`, `paste_lesson_usecase.py`, `rename_linked_file_for_row_usecase.py`: `build_ub_stem`-Aufrufe um den Titelparameter bereinigt. `_topic_from_lesson_stem` in `RenameLinkedFileForRowUseCase` entfernt (war nur für den UB-Titelextrakt nötig).
  - `kursplaner/infrastructure/repositories/ub_repository.py`: Fallback-Stem in `unique_ub_markdown_path` auf `"ub 00-00-00"` angepasst.
  - `tools/migrate_ub_filenames.py` (neu): Einmaliges Migrationsskript, das 10 bestehende UB-Dateien umbenennt, alle ``Unterrichtsbesuch``-YAML-Links in Einheitendateien mit Pflicht-Anführungszeichen aktualisiert und die UB-Übersicht neu generiert. Idempotent (zweiter Lauf findet nichts mehr).
  - Tests: Alle betroffenen Testdateien (`test_course_subject_and_ub_policy.py`, `test_mark_unit_as_ub_usecase.py`, `test_paste_lesson_usecase_ub_copy.py`, `test_rename_linked_file_for_row_usecase.py`, `test_query_ub_achievements_usecase.py`, `test_query_ub_plan_usecase.py`) auf neues Format angepasst; 14 neue Tests für `UbStem`-Verhalten.

### Added

- **Stundenplanänderung-Feature** (2026-08-06): Neue Aktion im Aktion-Menü ("Stundenplanänderung…") zum Umverteilender Unterrichtseinheiten auf einen veränderten Stundenplan.
  - `kursplaner/core/usecases/timetable_change_usecase.py`: `TimetableChangeUseCase.compute()` filtert alte Einheiten nach Datumsbereich, ruft `generate_rows()` mit neuem Stundenplan auf und verteilt Inhalte in Reihenfolge auf `DraftSlot`-Objekte. Exportiert benannte Prädikat-Wrapper `column_is_ferien`, `column_is_manual_ausfall`, `column_is_stattfindend` (Pflicht laut Architektur — keine Inline-Heuristiken). `was_recovered_week`-Flag kennzeichnet neue stattfindende Slots in Kalenderwochen mit manuellem Ausfall im alten Plan.
  - `kursplaner/core/usecases/apply_timetable_change_usecase.py`: `ApplyTimetableChangeUseCase.execute()` ersetzt den Datumsblock in `table.rows` durch die finalen `DraftSlot`-Zeilen und ruft `plan_repo.save_plan_table()` auf. Meldet verdrängte Wiki-Links (`dropped_contents`) zurück an den Adapter.
  - `kursplaner/adapters/gui/timetable_change_dialog.py`: `TimetableChangeDialog(ScrollablePopupWindow)` — Split-View (alter Plan links, neuer Plan rechts), Von/Bis-Datumsfelder, Wochentag/Stunden-Eingabe, Berechnen-Button, Ausfall/Stattfinden/↑/↓/Entfernen-Aktionen, Strg+Z für internen Dialog-Undo-Stack (`list[list[DraftSlot]]`, `copy.deepcopy`). Übernehmen ruft `on_accept`-Callback auf; Hauptfenster-Undo wird über `_run_tracked_write` als ein Batch-Eintrag erzeugt.
  - Intent-Verdrahtung: `OPEN_TIMETABLE_CHANGE` in `ui_intents.py`, Separator + Menüpunkt in `screen_builder.py`, Dispatch in `ui_intent_controller.py`, `open_timetable_change()`-Methode in `action_controller.py`.
  - Bootstrap: `TimetableChangeUseCase` und `ApplyTimetableChangeUseCase` in `wiring.py` als `GuiDependencies`-Felder verdrahtet.
  - Tests: 15 Tests in `tests/test_timetable_change_usecase.py` (Prädikat-Wrapper, compute-Logik inkl. Ferien/Ausfall/truncate/empty-Slots) und 7 Tests in `tests/test_apply_timetable_change_usecase.py` (Splice, Ferien/Ausfall/leer/dropped). Alle 22 Tests grün; vorbestehende 4 Failures in `test_selection_controller.py` unverändert.

### Fixed (technische Details, 2026-08-04)
- Ausfall-Erkennung ("Thema/Ausfall"-Spalte vs. Legacy-"Inhalt"-Spalte) war seit der 4-Spalten-Plantabellen-Migration an drei Stellen auseinandergedriftet: `PlanOverviewQueryUseCase.summarize_plan()` (falsches "Nächste Einheit"/Reststunden), `PlanCommandsUseCase.convert_to_ausfall()`/`restore_from_cancel()` (Schreib-/Lesepfad der "Als Ausfall markieren"-Aktion zielte auf die falsche Spalte) und `shift_existing_lessons_forward()` (konnte eine Einheit in eine Ferien-/Ausfallzeile verschieben). Neue zentrale Helper-Funktion `resolve_row_cancel_state(headers, row)` in `kursplaner/core/domain/content_markers.py` bündelt die schema-übergreifende Erkennung (bevorzugt `Thema/Ausfall`, Fallback auf Legacy-Inhalt-Marker nur bei fehlender Spalte); alle vier Stellen nutzen jetzt diese eine Implementierung. `load_plan_detail_usecase.py::build_day_columns()` (bereits korrekt) wurde ebenfalls auf den gemeinsamen Helper umgestellt.
- Oberthema für Einheiten ohne verlinkte Stunden-Datei: neue Funktion `extract_plan_oberthema()` in `kursplaner/core/domain/plan_table.py` liest das Oberthema als Fallback direkt aus der rohen `Thema/Ausfall`-Zelle (Umkehrung von `sync_thema_ausfall_to_plan_row`). `build_day_columns()` befüllt `day["plan_oberthema"]`; `GridCellPolicyUseCase.field_value()` und `topic_sequence_runs.row_oberthema()` fallen darauf zurück, wenn `yaml["Oberthema"]` (noch) leer ist. Dadurch erkennt `compute_topic_sequence_runs()` auch leere-aber-bereits-getaggte Einheiten korrekt als Kettenmitglied.
- Sequenz-Export-Tabelle wird jetzt auch vom automatischen Sync geschrieben: `SyncTopicSequencePlansUseCase` bekommt eine neue Abhängigkeit `sequence_export_sync: SyncSequenceExportTableUseCase` und ruft sie pro erkanntem Sequenz-Lauf mit ≥2 Mitgliedern auf. Die Zeilen-Aufbereitung (`TopicUnitExportRow`, `build_export_rows_for_run`, `EXPORT_TABLE_HEADERS`, `EXPORTABLE_LESSON_TYPES`) wurde aus `ExportTopicUnitsPdfUseCase` nach `kursplaner/core/domain/topic_sequence_runs.py` verschoben, damit manueller Export und Auto-Sync dieselbe Implementierung teilen. `wiring.py` reicht `sync_sequence_export_table_usecase` jetzt auch an `sync_topic_sequence_plans_usecase` durch.

### Performance
- Zoom-Debounce für `_on_grid_mousewheel()` (2026-08-04): Ctrl+Mausrad löste bisher pro Wheel-Event synchron einen vollen `_rebuild_grid()` aus, was schnelles Rauszoomen faktisch blockierte (jeder Tick wartete auf den vollen Rebuild des vorherigen). Neue Methoden `_schedule_zoom_rebuild()`/`_run_scheduled_zoom_rebuild()` in `grid_renderer.py` debouncen den Rebuild nach demselben `after`/`after_cancel`-Muster wie `ActionController.schedule_action_controls_update()` (120 ms); `day_column_width` aktualisiert weiterhin sofort.
- `grid_renderer.py` von ~823 auf ~709 Code-Zeilen reduziert: Sequenzfeld-Zeilenrendering (`_render_sequence_field_rows`/`_render_one_sequence_field_row`/`_bind_sequence_field_events`) nach `kursplaner/adapters/gui/sequence_field_grid_renderer.py` (`SequenceFieldGridRenderer`) ausgelagert — eigenständige Parameter statt freien Zugriffs auf den gesamten Grid-Rebuild-Zustand. `_rebuild_grid()` (234 Zeilen als eine Methode) mit `# deliberate exception: long by necessity` markiert. `docs/ARCHITEKTUR_KERN.md`-Ausnahmetabelle entsprechend aktualisiert; `wiring.py`s bereits vorher bestehende Zeilenzahl-Ausnahme dabei nachträglich dokumentiert (durch diese Session berührt, war aber vorher schon >300 Zeilen).
- Grid-Navigation per Fast Path beschleunigt (2026-08-04): `selection_controller.py:set_selected_cell()` aktualisiert jetzt bei normaler Navigation nur die 2 betroffenen Zell-Borders und max. 2 Spalten-Header statt das gesamte Grid — konform mit Architektur-Abschnitt 25c (partielle Updates statt Full-Rebuild). Neue Methode `_apply_single_header_style()` für Einzelspalten-Header-Styling. Row-Layout-Cache in `grid_renderer.py:_row_layout()` eingeführt (Invalidierung bei `_rebuild_grid()` und `collect_day_columns()`). Debounce für `update_action_controls()` in `action_controller.py` (80 ms, nur im Nav-Fast-Path). `parse_group_token()` in `lesson_context_controller.py` cacht per `id(current_table)`.
- Dateigrößen-Ausnahmen in `docs/ARCHITEKTUR_KERN.md` Abschnitt 28 dokumentiert (2026-08-04): vier Adapter-Dateien mit strukturell bedingter Überschreitung des 300-Zeilen-Limits benannt.

### Bekannte, nicht in dieser Session behobene Testbreakage
- `tests/test_selection_controller.py` (4 Tests) schlägt bereits vor dieser Session fehl: `_SelectionAppStub` hat kein `_is_rebuilding_grid`-Attribut, das `selection_controller.py::set_selected_cell()` seit dem Fast-Path-Commit (`4555a8f`, 2026-08-04) voraussetzt. Der Test-Stub wurde beim Hinzufügen dieses Attributs nicht mitgepflegt. Nicht behoben, da unabhängig von den vier hier bearbeiteten Bugs (Nächste-Einheit, Sequenz-Export-Sync, Zoom-Debounce, Oberthema-Fallback) — separates Ticket empfohlen.

### Changed
- Sequenzplanung-Foundation gestartet: neue Domain-/Infra-Module fuer Sequenzdateien (`kursplaner/core/domain/sequence_planning.py`, `kursplaner/infrastructure/repositories/sequence_plan_repository.py`) mit kanonischer Dateinamenbildung (`<Sequenz> <Lerngruppe> <26-2>`), `Kursplan`-YAML-Link und strikt teilweiser Update-Strategie (nur Brainstorming-Bereich editierbar, trailing Export-Tabelle ersetzbar).
- Persistente Datei-Relations-Registry eingefuehrt (`kursplaner/core/domain/file_relation_registry.py`, `kursplaner/infrastructure/repositories/file_relation_registry_repository.py`) und als Rebuild-Use-Case verdrahtet (`kursplaner/core/usecases/rebuild_file_relation_registry_usecase.py`, Composition Root in `kursplaner/adapters/bootstrap/wiring.py`).
- Registry-Rebuild in Runtime-Flows integriert: einmal beim App-Start (`kursplaner/adapters/gui/main_window.py`) sowie nach erfolgreichen getrackten Schreibvorgaengen (`kursplaner/adapters/gui/action_controller.py`), damit Plan-/Einheiten-/UB-/Sequenz-Beziehungen kontinuierlich aktualisiert bleiben.
- Kursübersicht erweitert: neue Spalte `Nächste Einheit` mit derselben Cutoff-Uhrzeitregel wie bei UB-Auswertungen; heutige Einheiten gelten ab konfigurierter Uhrzeit als vergangen.
- Kursübersicht-Filter ergänzt: ehemalige Kurse (nur vergangene Einheiten) sind standardmäßig ausgeblendet und können per neuer Übersichtsaktion/Shortcut (`Strg+Shift+E`) ein- oder ausgeblendet werden; Sonderfälle mit Ladefehlern/unklarer Datumsbasis bleiben sichtbar.
- Hervorhebung in der Kursübersicht konfigurierbar gemacht: Einträge mit naher nächster Einheit werden im einstellbaren Zeitfenster (Default 5 Tage) fett markiert.
- Toolbar-Aufbau entkoppelt: die bisherige Einheiten-Toolbar ist nicht mehr in der Kursübersicht sichtbar; stattdessen besitzt die Kursübersicht eine eigene Toolbar mit `Neuer Kurs` und `Ehemalige ein/aus`.
- Allgemeiner Soft-Guardrail fuer Shortcut-Abdeckung ergaenzt: `tools/ci/check_ai_guardrails.py` meldet lokal (non-blocking) Warnungen, wenn konfigurierte Kern-Intents (u. a. New/Export/Undo/Redo/Copy/Cut/Paste/Escape/Settings) ohne passenden Keyboard-Binding-Marker in Runtime-/Shortcut-Quellen gefunden werden.
- Phase-I-Decommission abgeschlossen: die zentralen UI-Contract-Bridges (`bw_libs/ui_contract/keybinding.py`, `bw_libs/ui_contract/popup.py`, `bw_libs/ui_contract/hsm.py`, `bw_libs/ui_contract/laufkern.py`) wurden auf schlanke Shared-Re-Exports reduziert; tote lokale Duplikat-Implementierungen sind entfernt.
- Guardrail-Decommission-Gate aktiviert: `tools/ci/check_ai_guardrails.py` erzwingt jetzt in allen vier Bridge-Modulen `ensure_bw_gui_on_path` + Shared-Import und blockiert lokale Re-Implementierungsmarker der Contract-Klassen/Funktionen.
- Wave-3-Sunset abgeschlossen: lokale `ModuleNotFoundError`-Fallback-Zweige wurden aus den zentralen UI-Contract-Bridges (`bw_libs/ui_contract/keybinding.py`, `bw_libs/ui_contract/popup.py`, `bw_libs/ui_contract/hsm.py`, `bw_libs/ui_contract/laufkern.py`) entfernt; Shared-Imports sind jetzt verpflichtend.
- Guardrail-Sunset auf Wave-3 umgestellt: `tools/ci/check_ai_guardrails.py` blockiert `except ModuleNotFoundError` jetzt repo-weit in den Scan-Scopes (`kursplaner`, `bw_libs`) ohne Bridge-Allowlist.
- Wave-2-Sunset-Gate aktiviert: `tools/ci/check_ai_guardrails.py` erlaubt `except ModuleNotFoundError` nur noch in den zentralen UI-Contract-Bridges (`bw_libs/ui_contract/keybinding.py`, `bw_libs/ui_contract/popup.py`, `bw_libs/ui_contract/hsm.py`, `bw_libs/ui_contract/laufkern.py`) und blockiert neue lokale Fallback-Zweige ausserhalb dieser Baseline.
- LaufKern-Bridge eingefuehrt: neues zentrales Modul `bw_libs/ui_contract/laufkern.py` (Shared-`bw_gui.laufkern`-Bridge mit lokalem Fallback) und Export ueber `bw_libs/ui_contract/__init__.py`; zusaetzlicher Regressionstest `tests/test_laufkern_bridge.py` fuer Manifestaufbau und Shortcut-basierte Reachability.
- LaufKern-Runtime-Auswertung in den produktiven Shortcut-Debug-Flow integriert: `kursplaner/adapters/gui/screen_builder.py` baut jetzt ein Manifest aus der Runtime-Shortcut-Registry, validiert es zentral und zeigt die aktuelle Intent-Reachability im Debug-Summary.
- LaufKern-Manifestaufbau aus dem Screen-Builder in einen dedizierten Provider ausgelagert: `kursplaner/adapters/gui/laufkern_manifest_provider.py` erzeugt jetzt den deklarativen Runtime-Manifestzustand aus der Shortcut-Registry.
- LaufKern-Tracking an produktiven Intent-Dispatch angebunden: `kursplaner/adapters/gui/main_window.py` protokolliert verarbeitete UI-Intents jetzt als LaufKern-Tracking-Artefakte (done/failed), und `screen_builder.py` zeigt zusaetzlich den Completion-Status aus der Artefaktaggregation im Runtime-Debug.
- Step-6-Guardrail-Hardening umgesetzt: `tools/ci/check_ai_guardrails.py` blockiert jetzt zusaetzlich repo-weit lokale Neudefinitionen der reservierten Shared-Primitives `TkRootHost`, `ScrollablePopupWindow` und `WrappedTextField`; der verbleibende Adapter `kursplaner/adapters/gui/popup_window.py:ScrollablePopupWindow` bleibt explizit als Kompatibilitaets-Allowlist markiert.
- Step-5-Rollout gestartet: `kursplaner/adapters/gui/main_window.py` nutzt jetzt den zentralen Runtime-Host `bw_gui.runtime.TkRootHost`; `kursplaner/adapters/gui/popup_window.py` und `kursplaner/adapters/gui/wrapped_text_field.py` binden die neuen Shared-Bausteine `bw_gui.dialogs.ScrollablePopupWindow` und `bw_gui.widgets.WrappedTextField` statt lokaler Eigenimplementierungen ein.
- Step-2-Guardrail-Scope abgeschlossen: die repo-weite GUI-Vertragspruefung in `tools/ci/check_ai_guardrails.py` umfasst jetzt zusaetzlich `bw_libs/`, sodass direkte `tkinter`/`ttk`-Imports und neue lokale `ui`/`widgets`/`tui`-Basisklassen auch in Shared-Library-Pfaden blockiert werden.
- Step-3-Exemption-Abbau abgeschlossen: verbleibende Klassen-Exemptions fuer `kursplaner/adapters/gui/main_window.py:KursplanerApp`, `kursplaner/adapters/gui/popup_window.py:ScrollablePopupWindow` und `kursplaner/adapters/gui/wrapped_text_field.py:WrappedTextField` durch Kompositionsmigration auf shared runtime-root/widgets entfernt.
- Step-3-Exemption-Abbau fortgesetzt: Future-Entrypoint-Baselines fuer `kursplaner/adapters/gui/main_window.py` und `kursplaner/adapters/gui/screen_builder.py` entfernt; aktiver Exemption-Backlog enthaelt nur noch die verbleibenden Klassenmigrationen.
- Step-3-Exemption-Governance aktiviert: `docs/GUI_MIGRATION_BACKLOG.md` ist jetzt verbindliche Referenz fuer aktive GUI-Baselines/Exemptions inkl. `remove_by`-Datum; Guardrails validieren die Backlog-Referenzen explizit.
- Governance-Policy geschaerft: `AGENTS.md` und `.github/copilot-instructions.md` enthalten jetzt explizit die Strict-bw-gui-only-Regel (keine lokale tkinter/ttk-Widgetimplementierung in Repos; wiederverwendbare GUI-Bausteine zuerst in bw-gui).
- Repo-weite Strict-bw-gui-Guardrails eingefuehrt: `tools/ci/check_ai_guardrails.py` scannt jetzt alle GUI-Pythondateien unter `kursplaner/adapters/gui/` per AST, blockiert direkte `tkinter`/`ttk`-Imports und verhindert neue lokale Basisklassen auf `ui`/`widgets`/`tui` (mit expliziter Legacy-Allowlist fuer bestehende Klassen).
- Future-App-Guardrails erweitert: `tools/ci/check_ai_guardrails.py` prueft jetzt zusaetzlich neue GUI-Entrypoint-Dateien unter `kursplaner/adapters/gui/` auf verpflichtenden Shared-GUI-Bootstrap (`ensure_bw_gui_on_path`, `bw_gui.runtime`, Shared-Menu/Dialog/Shortcut/Hover) und blockiert direkte `tkinter`-Imports.
- Guardrails gehaertet: `tools/ci/check_ai_guardrails.py` erzwingt Shared-UI-Vertraege in `kursplaner/adapters/gui/screen_builder.py` und `kursplaner/adapters/gui/hover_tooltip.py` und blockiert Legacy-Fallbackzweige (native Menuepfade, lokale Tooltip-Fallbackklasse).
- Nicht-Theme-Sonderpfade entfernt: `kursplaner/adapters/gui/screen_builder.py` und `kursplaner/adapters/gui/hover_tooltip.py` nutzen Shared-Menueleiste, Shared-Shortcut-Formatter und Shared-HoverTooltip jetzt verpflichtend; native Menue- und No-op-Tooltip-Fallbacks wurden geloescht.
- Theme-Sonderpfade entfernt: `kursplaner/adapters/gui/ui_theme.py` importiert Shared-Themeregister und Baseline-ttk-Setup jetzt verpflichtend; optionale Fallback-Branches (`ModuleNotFoundError`/`None`-Guards) wurden geloescht.
- Shared-Settings-Migration umgesetzt: `kursplaner/adapters/gui/settings_window.py` rendert die Einstellungen jetzt ueber `bw_gui.dialogs.open_tabbed_settings_dialog` (Sektionen fuer Pfade, UB-Cutoff und Lesson-Builder-Felder), inklusive bestehender Pfadvalidierung/Speicherflow aus dem Adapter.
- `kursplaner/adapters/gui/path_settings_controller.py` oeffnet den Settings-Flow jetzt direkt ueber den neuen Shared-Dialog-Entry-Point statt ueber ein lokales Popup-Window.
- Hover-Overlay-Formatierung harmonisiert: `kursplaner/adapters/gui/screen_builder.py` nutzt jetzt intent-basierte Hover-Text-Aufloesung ueber `bw_gui.shortcuts.compose_hover_text_for_intent(...)`, sodass Shortcut-Hinweise aus der zentralen Runtime-Registry stammen.
- Shared-Tooltip-Polish uebernommen: `bw_gui.widgets.HoverTooltip` nutzt jetzt verzogertes Anzeigen, Theme-Ableitung aus dem aktiven Fensterkontext sowie Bildschirm-Clamping/Cancel-Handling fuer stabilere Hover-Overlays.
- Shared-Theme-Feinschliff uebernommen: gemeinsame ttk-Styles fuer Settings-Panels/Hint-Labels und verfeinerte Scrollbar-Kontraste kommen jetzt aus dem aktualisierten `bw-gui`-Submodule-Stand.
- Shared-Themeregister vereinheitlicht: `kursplaner/adapters/gui/ui_theme.py` mischt jetzt die zentrale `bw_gui`-Theme-Registry ein, sodass Kursplaner- und Blattwerk-Themefamilien gemeinsam verfuegbar sind.
- Alt-Navigation im Shared-Hauptmenue visuell wiederhergestellt: Top-Level-Menuebuttons zeigen die Mnemonic-Unterstreichung wieder konsistent im CustomMenuBar-Pfad.
- Shared-Menueleiste integriert: `kursplaner/adapters/gui/screen_builder.py` baut das Hauptmenue jetzt primaer ueber `bw_gui.menu.CustomMenuBar` mit zentralen `MenuDefinition`/`MenuItem`-Providern (inkl. Theme-/Undo-Untermenues) und behaelt einen nativen `ui.Menu`-Fallback fuer fehlende Shared-Menu-Module.
- Tk-Runtime-Pilotmigration erweitert: `kursplaner/adapters/gui/window_identity.py` nutzt jetzt `bw_gui.runtime.ui`-Typen/Exceptions statt direktem `tkinter`-Import.
- Tk-Runtime-Pilotmigration erweitert: `kursplaner/adapters/gui/path_bootstrap.py` nutzt jetzt `bw_gui.runtime.ui` fuer den Root-Window-Start statt direktem `tkinter`-Import.
- Tk/ttk-Runtime-Pilotmigration erweitert: `kursplaner/adapters/gui/column_visibility_dialog.py` nutzt jetzt zentrale Runtime-Aliases aus `bw_gui.runtime` (`ui`/`widgets`) statt direkter `tkinter`-/`ttk`-Imports.
- Tk/ttk-Runtime-Pilotmigration erweitert: `kursplaner/adapters/gui/wrapped_text_field.py` nutzt jetzt zentrale Runtime-Aliases aus `bw_gui.runtime` (`ui`/`widgets`) statt direkter `tkinter`-/`ttk`-Imports.
- Tk/ttk-Runtime-Pilotmigration erweitert: `kursplaner/adapters/gui/export_selection_dialog.py` und `kursplaner/adapters/gui/ub_mark_dialog.py` nutzen jetzt zentrale Runtime-Aliases aus `bw_gui.runtime` (`ui`/`widgets`) statt direkter `tkinter`-/`ttk`-Imports.
- Tk/ttk-Runtime-Pilotmigration erweitert: `kursplaner/adapters/gui/lzk_column_dialog.py` und `kursplaner/adapters/gui/shortcut_overview_dialog.py` nutzen jetzt zentrale Runtime-Aliases aus `bw_gui.runtime` (`ui`/`widgets`) statt direkter `tkinter`-/`ttk`-Imports.
- Tk/ttk-Runtime-Pilotmigration erweitert: `kursplaner/adapters/gui/settings_window.py` nutzt jetzt zentrale Runtime-Aliases aus `bw_gui.runtime` (`ui`/`widgets`) statt direkter `tkinter`-/`ttk`-Imports.
- Tk/ttk-Runtime-Pilotmigration erweitert: `kursplaner/adapters/gui/new_lesson_window.py` nutzt jetzt zentrale Runtime-Aliases aus `bw_gui.runtime` (`ui`/`widgets`) statt direkter `tkinter`-/`ttk`-Imports.
- Tk/ttk-Runtime-Pilotmigration erweitert: `kursplaner/adapters/gui/popup_window.py` nutzt jetzt zentrale Runtime-Aliases aus `bw_gui.runtime` (`ui`/`widgets`) statt direkter `tkinter`-/`ttk`-Imports.
- Tk/ttk-Runtime-Pilotmigration erweitert: `kursplaner/adapters/gui/lesson_builder_dialog.py` nutzt jetzt zentrale Runtime-Aliases aus `bw_gui.runtime` (`ui`/`widgets`) statt direkter `tkinter`-/`ttk`-Imports.
- Tk/ttk-Runtime-Pilotmigration erweitert: `kursplaner/adapters/gui/overview_controller.py` nutzt jetzt zentrale Runtime-Aliases aus `bw_gui.runtime` (`ui`/`widgets`) statt direkter `tkinter`-/`ttk`-Imports.
- Tk/ttk-Runtime-Pilotmigration erweitert: `kursplaner/adapters/gui/selection_overlay_controller.py` nutzt jetzt zentrale Runtime-Aliases aus `bw_gui.runtime` (`ui`/`widgets`) statt direkter `tkinter`-/`ttk`-Imports.
- Tk-Runtime-Pilotmigration erweitert: `kursplaner/adapters/gui/toolbar_icon_styler.py` nutzt jetzt zentrale Runtime-Aliases aus `bw_gui.runtime` (`ui`) statt direktem `tkinter`-Import.
- Tk-Runtime-Pilotmigration erweitert: `kursplaner/adapters/gui/hover_tooltip.py` nutzt jetzt die Shared-Core-Bridge ohne direkten `tkinter`-Import.
- Tk/ttk-Runtime-Pilotmigration erweitert: `kursplaner/adapters/gui/ui_intent_controller.py` nutzt jetzt zentrale Runtime-Aliases aus `bw_gui.runtime` (`ui`/`widgets`) statt direkter `tkinter`-/`ttk`-Imports.
- Tk-Runtime-Pilotmigration erweitert: `kursplaner/adapters/gui/grid_renderer.py` nutzt jetzt zentrale Runtime-Aliases aus `bw_gui.runtime` (`ui`) statt direktem `tkinter`-Import.
- Tk/ttk-Runtime-Pilotmigration erweitert: `kursplaner/adapters/gui/ui_theme.py` nutzt jetzt zentrale Runtime-Aliases aus `bw_gui.runtime` (`ui`/`widgets`) statt direkter `tkinter`-/`ttk`-Imports.
- Tk-Runtime-Pilotmigration erweitert: `kursplaner/adapters/gui/main_window.py` nutzt jetzt zentrale Runtime-Aliases aus `bw_gui.runtime` (`ui`) statt direktem `tkinter`-Import.
- Tk/ttk-Runtime-Pilotmigration erweitert: `kursplaner/adapters/gui/screen_builder.py` nutzt jetzt zentrale Runtime-Aliases aus `bw_gui.runtime` (`ui`/`widgets`) statt direkter `tkinter`-/`ttk`-Imports.
- Tk/ttk-Runtime-Pilotmigration erweitert: `kursplaner/adapters/gui/action_controller.py` nutzt jetzt zentrale Runtime-Aliases aus `bw_gui.runtime` (`ui`/`widgets`) statt direkter `tkinter`-/`ttk`-Imports.
- Tk-Runtime-Pilotmigration erweitert: `bw_libs/app_shell.py` nutzt jetzt `bw_gui.runtime.ui` statt direktem `tkinter`-Import.
- Shared-Dialogmigration gestartet: zentrale Bridge `kursplaner/adapters/gui/dialog_services.py` auf `bw_gui.dialogs` eingefuehrt und mehrere Controller (`action_controller.py`, `lesson_conversion_controller.py`, `overview_controller.py`, `path_settings_controller.py`, `editor_controller.py`, `selection_controller.py`, `column_reorder_controller.py`) von direkten `tkinter`-Dialogimports auf Shared-Services umgestellt.
- Shared-Dialogmigration erweitert: auch `new_lesson_window.py`, `lesson_builder_dialog.py`, `settings_window.py`, `ub_mark_dialog.py`, `popup_window.py` und `path_bootstrap.py` nutzen jetzt die zentrale Dialog-Bridge statt direkter `tkinter`-Dialogimports.
- Pilotmigration zum gemeinsamen GUI-Core gestartet: `bw-gui` als Git-Submodule eingebunden und `bw_libs/ui_contract/*` via Bridge auf `bw_gui.contracts.*` umgestellt, sodass Keybinding-/Popup-/HSM-Vertraege aus der gemeinsamen Quelle geladen werden.
- Erste UI-Flow-Uebernahme aktiv: `kursplaner/adapters/gui/hover_tooltip.py` nutzt jetzt die gemeinsame `bw_gui.widgets.HoverTooltip`-Implementierung, und `ui_theme.py` wendet eine gemeinsame Theme-Baseline vor kursplanerspezifischen Style-Overlays an.
- Shortcut-Hinweis aus dem Button-Label entfernt: `Zur Kursliste` zeigt kein `(Esc)` mehr; Shortcut-Hinweise bleiben im Hover-Overlay (`_add_help`).
- Runtime-Workspace-Root-Aufloesung fuer UB-/Plan-Use-Cases entkoppelt: statt festem Ordnernamen `7thCloud` nutzt die GUI-/Use-Case-Schicht jetzt eine zentrale, generische Root-Inferenz (`infer_workspace_root_from_path`) im Pfadmodul.
- G5 abgeschlossen: AppIdentity-Manifest `kursplaner/app_info.py` eingefuehrt und im Composition-Root als `app_info`/`shell_config`-Single-Source fuer Startup-Metadaten verdrahtet.
- G3/G4 gestartet: `build_gui_dependencies()` liefert jetzt auch eine zentrale Shell-Konfiguration; `KursplanerApp` verwendet injizierte `AppDependencies` und initialisiert das Hauptfenster ueber `bw_libs/app_shell.py`.
- G2.3 erweitert: weitere Repository-Write-Pfade nutzen jetzt zentrale Atomic-APIs (`plan_table_file_repository.py`, `plan_repository.py`, `ub_repository.py`, `lesson_file_repository.py`, `subject_source_repository.py`).
- G2.2 erweitert: `kursplaner/core/config/app_state_store.py` und `kursplaner/core/usecases/daily_course_log_usecase.py` nutzen jetzt die zentrale `atomic_write_json`-API.
- G2.1 gestartet: Shared-Modul `bw_libs/app_paths.py` eingefuehrt (AppPaths-Discovery sowie atomische JSON/Text-Write-Helfer).
- Persistenz-Pilot: `kursplaner/core/config/path_store.py` und `kursplaner/core/config/ui_preferences_store.py` nutzen jetzt die zentrale `atomic_write_json`-API.
- Guardrails beruecksichtigen `bw_libs/app_paths.py` als relevanten Shared-Pfad.
- UI-Contracts fuer Keybindings, Popup-Lifecycle und HSM wurden auf das Shared-Paket `bw_libs/ui_contract/` umgestellt; GUI und Tests importieren die Vertraege jetzt zentral statt aus lokalen Duplikatmodulen.
- Guardrails/Governance wurden auf `bw_libs/ui_contract`-Pfade umgestellt; `bw_libs/` wird bei Changelog-/Development-Log-Relevanz mitgeprueft.
- `kursplaner/adapters/gui/ui_intent_controller.py` validiert Intents jetzt gegen einen zentralen HSM-Contract vor der Intent-Delegation.
- Escape-Handling im Intent-Controller folgt jetzt zentraler Prioritaet: aktives Popup schliessen, dann Detail-Child-Zustaende abbauen, danach Rueckkehr in den Parent-State.
- Runtime-Debug-Popup wurde als nicht mode-blockierendes Parallel-Popup (`dialog.non_blocking`) verdrahtet; der Shortcut-Resolver behandelt nur noch mode-blockierende Popups als Dialog-Prioritaet.
- Wave-1-Hardening fuer Popup-Lifecycle: `kursplaner/adapters/gui/screen_builder.py` synchronisiert Laufzeit-Popups jetzt ueber `PopupPolicyRegistry` und nutzt die Registry im Runtime-Kontext als primaere Dialogquelle.
- Runtime-Dispatch konsolidiert: popup-sensitive Shortcut-Pfade pruefen zentral `_has_active_popup()` statt verteilter Einzelabfragen.
- Guardrails erweitert: `tools/ci/check_ai_guardrails.py` validiert jetzt zusaetzlich die tatsaechliche Runtime-Integration (`evaluate_runtime`, PopupPolicy-Nutzung) in `screen_builder.py`.
- Wave-1-Start fuer den Hybrid-Resolver: `kursplaner/adapters/gui/keybinding_registry.py` enthaelt jetzt einen zentralen Runtime-Kontextvertrag (`KeybindingRuntimeContext`) und eine einheitliche `evaluate_runtime`-API fuer mode-/offline-/textfokus-/dialogbasierte Aktivierungspruefung.
- Wave-1 konkret verdrahtet: globale Shortcuts in `kursplaner/adapters/gui/screen_builder.py` laufen jetzt ueber den zentralen Runtime-Resolver statt direkter Bind-Dispatches.

### Added
- HSM-Contract-Modul `kursplaner/adapters/gui/hsm_contract.py` eingefuehrt (Intent-/Payload-Validierung, Transition-Regeln, Escape-Resolver).
- Neue Tests `tests/test_hsm_contract.py` fuer Intent-Contract, Transition-Gates und Escape-Prioritaetskette.
- Tests fuer zentrale Runtime-Module: `tests/test_keybinding_runtime_registry.py` (Mode-/Reason-Matrix) und `tests/test_popup_policy_registry.py` (Stack-/Manifest-Lifecycle).
- Tabellarische Runtime-Debug-Ansicht fuer Shortcuts in der Hauptansicht (Menuepunkt `Ansicht -> Shortcut-Runtime-Debug`, Shortcut `Strg+Shift+D`) inkl. Offline-Simulation (`Strg+Shift+O`) und Aktiv/Disabled-Gruenden pro Modus.
- Guardrails praezisiert: `CHANGELOG.md` wird nun bei nutzer- oder coentwicklerrelevanten Aenderungen erzwungen; Prozesswarnungen (Commit-/Push-Guidance) werden nur noch lokal und nicht in CI ausgegeben.
- Zentrale UI-Governance gestartet: `kursplaner/adapters/gui/keybinding_registry.py` und `kursplaner/adapters/gui/popup_policy.py` als gemeinsame API-Basis fuer Shortcut- und Popup-Steuerung eingefuehrt.
- Guardrails erweitert: AGENTS/Copilot/PR-Template verlangen zentrale Shortcut-/Popup-Registrierung sowie Feature-Commit-Disziplin bei manuellem Push.
- `tools/ci/check_ai_guardrails.py` prueft die Existenz der neuen Zentralmodule und meldet Commit-/Push-Prozessdrift als non-blocking Warnung.
- Paste-Konfliktfall `delete` bereinigt jetzt auch UB-Artefakte der ersetzten Zieleinheit: verknuepfte UB-Datei wird entfernt und UB-Uebersicht im selben Writeflow aktualisiert; alle betroffenen Pfade sind fuer Undo/Redo im Ergebnismodell markiert.
- Guardrail-Absicherung durch dedizierte Tests erweitert: Negativfall (direkter `apply_value`-Deletepfad) und Positivfall (getrackter Writeflow) sind als Unit-Tests fuer die AST-Guardrail-Regel abgedeckt.
- Spaltenmodus `Strg+X` ist jetzt als echter Einheit-Cut verdrahtet: statt Zelltext-Cut wird eine verlinkte Einheit zum Verschieben vorgemerkt.
- Cut+Paste fuer Einheiten mit UB als Move-Flow umgesetzt: beim Einfuegen wird die Ziel-Einheit geschrieben, danach die Quell-Einheit samt alter UB-Datei aufgeraeumt; Ergebnis ist eine verschobene Einheit mit UB-Update statt Duplikat.
- Einheits-Loeschen fuer Unterricht mit UB auf transaktionalen Writeflow umgestellt: Einheit-Datei wird beim Loeschen entfernt, optional verknuepfte UB-Datei mitgeloescht, UB-Uebersicht im selben Zyklus synchronisiert.
- Delete-Shortcut im Grid (`Inhalt`) routed jetzt auf denselben Action-Writeflow wie Toolbar-Loeschen statt direktem Zell-Write, damit Undo/Redo alle beteiligten Dateien konsistent rueckspielen kann.
- Paste-Flow erweitert um Pflichtauswahl bei Quellen mit UB-Verknuepfung: vor Einfuegen muss entschieden werden `UB mitkopieren` / `ohne UB` / `abbrechen`; bei Mitkopieren wird eine neue UB-Datei fuer das Ziel erzeugt und die Uebersicht aktualisiert.
- AI-Guardrails um AST-Pruefungen fuer Undo-kritische Writeflows erweitert (Delete/Paste muessen getrackt laufen; UB-Kopierdialog im Paste-Flow verpflichtend).
- Strg+Enter fuer Spaltenmodus ausgebaut: im Spaltenauswahlmodus oeffnet jetzt ein typabhaengiger Bestaetigungsdialog (Unterricht, LZK, Ausfall, Hospitation) statt des reinen Edit-Commits.
- Unterricht-Dialog bei Strg+Enter nutzt denselben Builder wie bei neuer Planung, aber mit Voll-Prefill aus bestehender Stunde (YAML + Inhalte/Methodik-Abschnitte aus Markdown), sodass bestehende Spaltenwerte direkt bearbeitbar sind.
- Ausfall/Hospitation auf bestehendem Dialogmuster belassen und um Vorbelegung erweitert (Ausfallgrund aus Markertext, Beobachtungsschwerpunkt aus YAML).
- Neues separates LZK-MVP-Fenster fuer Strg+Enter hinzugefuegt; optionaler Titel-Override, anschliessend bestehender LZK-Write-Flow.
- Escape-Verhalten in Popup-Basisklasse verfeinert: Esc schliesst nur sofort bei Popup-Fokus; bei Fokus in editierbaren Eingabefeldern wird zunaechst auf Popup-Ebene fokussiert.
- Popup-Modalfokus gehaertet: `_activate_modal_focus` bricht jetzt ab, wenn das Fenster nicht das aktive Top-Popup ist, um Fokus-Rueckspruenge in verschachtelten Popup-Flows zu vermeiden.
- UI-Persistenz erweitert: neue Preferences `lesson_builder_fields.show_kompetenzen` und `lesson_builder_fields.show_stundenziel` steuern die Sichtbarkeit optionaler Builder-Felder.
- Settings-Dialog um Feldsichtbarkeit fuer `Einheit planen` erweitert; Speicherung erfolgt zentral ueber `ui_preferences_store`.
- Lesson-Builder flexibilisiert: `Kompetenzen`/`Stundenziel` koennen ausgeblendet werden; `Stundenziel` ist nicht mehr als Pflichtauswahl erzwungen.
- LZK-Erkennung im Kern entheuristisiert: Detail-Projection, Daily-Log, Plan-Overview und LZK-Nummerierung nutzen jetzt YAML-`Stundentyp` statt String-Treffer auf Titel/Inhalt.
- Lesson-Index-Metadaten fuer Overview erweitert um `Stundentyp`, damit indexbasierte Abfragen ohne Topic-Heuristik auskommen.
- UB-Fokus in der GUI ausgebaut: Kursuebersicht zeigt jetzt `Naechster UB` im Format `D.M. Initialen+` (leer ohne zukuenftigen UB), Einheiten mit UB erhalten eine theme-abhaengige Vollspalten-Umrandung.
- UB-Interaktion umgestellt: erneuter Klick auf UB oeffnet den UB-Dialog zur Bearbeitung (mit Vorbelegung) statt sofortigem Entfernen; Loeschen erfolgt jetzt explizit ueber eigene Dialogaktion.
- UB-Ansicht als Tab-UI neu strukturiert: `Achievements`, `UB-Plan` (kommende/absolvierte Listen mit Spalten `Datum`, `Faecher`, `+`, `Kurs`) und `Entwicklungsimpulse`; Tabwechsel per Maus und Pfeiltasten.
- HO/LZK-Erzeugung vereinheitlicht: neue Hospitationen speichern in der Kurstabelle nur noch den Markdown-Link, HO-Dateithemen werden ohne doppelte Lerngruppen-Nennung gesetzt, und LZK-Titel werden mit Fachkuerzel statt Vollfach erzeugt.
- Hospitations-Detailmodus erweitert: `Stundenthema` ist jetzt im Hospitationsmodus sichtbar, analog zur Themenanzeige bei Unterrichtseinheiten.
- Dokumentrollen formal getrennt: Architektur-Referenz (`docs/ARCHITEKTUR_KERN.md`) vs offene Arbeit (`docs/ARCHITEKTUR_UMSETZUNGSPLAN.md`) vs Verlauf (`docs/DEVELOPMENT_LOG.md`).
- Guardrail-Regeln erweitert um verpflichtendes Development-Log-Update fuer relevante Aenderungen.
- Repo-Path-Guardrails repariert: CI verwendet jetzt wieder ein vorhandenes Pruefskript (`tools/repo_ci/check_no_absolute_paths.py`) fuer absolute JSON-Pfade.
- AI-Guardrails erweitert, damit Workflow-/Script-Drift fuer Repo-Path-Checks frueh erkannt und lokal blockiert wird.
- ttk-Themekonfiguration erweitert: `TScrollbar`, `Horizontal.TScrollbar` und `Vertical.TScrollbar` werden zentral modern gestylt, damit Scrollbereiche in allen GUI-Teilen konsistent zur Theme-Sprache passen.

### Added
- `CHANGELOG.md` fuer oeffentliche, nutzerorientierte Kommunikation.
- `.github/pull_request_template.md` mit Pflichtfeldern fuer Doku- und Release-Kommunikation.

## [History]

### 2026-04-12
- Development-Log-Governance eingefuehrt und in Guardrails verankert.
